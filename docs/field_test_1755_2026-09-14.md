# 2026-09-14 17:55 이후 0.2.126 실기 로그 조사

## 출처
2026-09-14 17:59:12 KST NAS 확인 후 gnlink 읽기 전용 SCP로 수집. 증거 `.claude/investigate-1755/nas/` + SHA256SUMS.txt. 원본은 커밋하지 않는다.
- 회사 Viewer machine68f79d01, version0.2.126. 게임용 hostId=df15b73b18cda3b1(machine51bd6657).
- 게임 session38920-630234500: 시작17:55:23, Hello/버전교환17:55:26, 종료17:57:43.444 done. 경로선택175.207.45.151:29190(릴레이 endpoint; 서버중계량은 이번에 별도집계하지 않음).
- 채팅 session25580-630375500:17:57:45~17:58:43 수집본, 대상집hostId=f6e8e54a7d5aff7c. 실제키 이벤트 구간17:57:59.070~17:58:41.785.
- 회사 host.log NAS mtime17:49, 마지막17:49:16.787: 이후 호스트 내부 로그 미확보. 업로드 누락으로 호스트 프로세스 종료를 추정하지 않는다.

## 실제 개선과 잔여 지연
게임 표시 간격7422개: p50 15.827ms, p95 32.524ms, max1134.467ms. 1초 초과는4번, 각각 이후 표시가 재개되고 세션 done까지 이어짐. 이 약137초 관측에서는 지속 정지가 확인되지 않음(장시간 무정지 보증 아님).

| 시각(KST, 수집로그) | seq | 표시공백 ms | publish 준비→paint 시작 ms | 캡처→encode ms | encode ms | decode ms |
|---|---:|---:|---:|---:|---:|---:|
|17:55:31.470|128|1134.467|1134.630|34.256|3.401|2.905|
|17:55:44.627|887|1091.261|1091.475|27.852|3.142|5.266|
|17:55:52.746|1360|1118.799|1118.946|34.049|4.480|16.961|
|17:57:38.095|7356|1113.822|1113.977|31.010|3.128|4.476|

- **국소화**: 표시된 이 네 프레임의 긴 지연 대부분은 뷰어 클록의 queueSet→paintStart 구간이다. capGap 명칭 추정이 아니라 실제 같은 장치의 원시시각 차분. 단 queueSet은 viewer_video_receiver_frame.cpp에서 frame.mu 취득 **전**이므로 게시 잠금 대기와 UI 메시지/그리기 시작 대기를 합친 값이다. 'UI 메시지큐만의 원인' 또는 GPU문제 확정은 아님.
- 이후 paint 자체는0.24~0.28ms. 해당프레임 capture→encode28~34ms, encode3~4.5ms, decode3~17ms. 이 프레임들의1.1초를 host encode 소요시간으로 설명할 수 없음.
- 게임 전체 control RTT 표본 p50 3.602ms/p95 4.835ms/max1096.905ms. 긴표시대기와 control 지연이 동반돼 시스템스케줄링/공유잠금/수신 스레드 영향을 추가구분해야 함. RTT를 순수회선지연으로 단정하지 않음.
- recv7296/decoded7260, skipped36(약0.49%). assembly completed7295/dropped41(약0.56% frame assembly실패), keyReq8. 정상범위가대부분이나 네트워크가 전부 무관하다고 말하지 않음.
- eventQpc→collectedTick의 기준오프셋 제거 상대지연 max약19ms. 1초 parent stdout 수집적체를 입증하는 표본은 없음. logMu 취득 전/OS 스케줄링은 이 지표의 범위 밖.

## 빠른 정적 타이핑
채팅 세션 키/physical-key 계열663개: 큐대기 p50 0.011ms/p95 0.624ms/max49.592ms, 전송→응답 p50 2.015ms/p95 2.655ms/max5.375ms, 교환실패0. 사용자의 빠른 반응 보고와 일치. 이는 ACK까지이며 실제 글자표시까지의시간보증은 아님.
채팅세션1.53/1.31초 표시공백은17:57:49/52로 실제타이핑 시작17:57:59보다 앞선 연결초기였다. 정적화면의표시간격 자체를 키반응 지연으로 해석하지 않는다.

## 별도 발견: 게시 Stream 버전 표찰 불일치 (REL-V01)
- 새로그는 game/home 모두 peerVersion=0.2.125를 응답하지만 version exchange를 지원함.
- Codex가 **0.2.126 서명 manifest의 원본payload GNLinkStream.exe**를 직접 검사: UTF16 `0.2.125` 1개, `0.2.126` 0개. 같은패키지Viewer/Host는126. 125정식Stream에는version-exchange기능/버전문자열이 없었음.
- 따라서 이peer보고만으로 '사용자Host가125설치상태'라고 단정하면 잘못이다. 배포패키지 자체에 낡은 실행버전표찰이 남아 있음. 이는 Codex의 빌드/검증 공백이며 해시/서명일치와 별개 결함.
- 빌드 중 product_version.hpp 변경과 증분오브젝트 재사용이 유력 발생기전(작업이력상 확인, 정확객체역추적은 추가 필요). 수정방향: 버전확정→소스동결 후 clean build, **패키지 바이너리가 실제교환하는 버전과 manifest버전 일치**를 게시전 런타임 gate로 추가. 단순소스버전/파일해시검증만으론 못 잡음.
- 이번로그의프레임/입력계측은존재하므로지연분석은계속가능. 실행설치본전량hash는별도이며혼합설치여부확정하지 않음. 자동설치/재시작/롤백은하지않음.

## 다음 조사·수정 지점
1. 긴4구간: frame.mu 취득전/후·publish 완료시각, UI 메시지dispatch 시작/종료·paint 요청/실제실행, 장시간락/드라이버호출을 분리해1.1초를막는함수를확인. 현재계측은게시준비→paint까지국소화했으나정확한블로킹함수미확정.
2. 별도 REL-V01 빌드버전표찰 정정 및 런타임 버전 gate. 사용중인좋아진세션은무접촉.
3. 검증용에 동일증거독립대조 요청(seq2301). 이번문서수치는Codex 직접계산, 상세stage는지연시우선추출되는표본이라전체프레임분포로일반화하지않음.
