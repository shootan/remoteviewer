# GNLink 구현계획 (원격 접근 신뢰성 단계)
Created: 2026-08-04
Baseline: `v0.2.2` (dist `GNLink-0.2.2.apk`, `GNLinkSetup-0.2.2.exe`)

이 문서를 앞으로 작업의 단일 기준(Source of Truth)으로 사용한다.
이전 성능/화질/Android 마일스톤(M0~M8) 기록은 `docs/구현계획_old.md`에 보관되어 있다.

## 문서 운영 규칙
- 이 문서는 체크리스트/우선순위/완료조건만 유지한다.
- 완료 표시는 체크박스와 상태 갱신으로만 반영한다.
- 작업 이력/실행 로그/회고는 `docs/history.md`에만 기록한다.
- 항목 ID(`N1`, `S2` 등)는 재사용하지 않는다. 폐기 시 `[취소]`로 남긴다.

---

## 0) 이 단계의 목표

M0~M8에서 **화면은 잘 나오게** 만들었다. 이 단계의 목표는 **실제로 접속되고, 접속된 뒤에 할 일을
다 할 수 있게** 만드는 것이다. 남은 격차는 성능이 아니라 도달성과 권한이다.

- **도달성**: 제한적인 네트워크(회사/게스트 Wi-Fi, 대칭 NAT)에서 접속된다.
- **권한**: UAC 동의 창을 보고 누를 수 있다. 현재는 둘 다 불가능하다.
- **정확성**: 실패가 실패로 보인다. 지금은 여러 경로에서 실패가 성공처럼 보고된다.

---

## 1) 근거 대장 (측정 vs 가정)

작업 우선순위가 여기에 달려 있어 분리해 둔다. **가정을 측정으로 바꾸는 항목이 가장 싸고 가장 중요하다.**

| # | 명제 | 상태 | 근거 |
|---|---|---|---|
| E1 | 회사 Wi-Fi에서 UDP 43000 아웃바운드가 막힌다 | **측정됨** | 디렉토리 UDP 8081 관측은 12회 전부 성공(pendingPunch 생성), 호스트 43000 도달 0건(`peer punch` 로그 0). 같은 폰이 LTE에서는 첫 시도에 접속 |
| E2 | 이 공유기는 NAT 포트를 보존한다 | **측정됨** | `bindPort=43000` → `public=...:43000` |
| E3 | UAC는 secure desktop(Winlogon)에 그려진다 | **측정됨** | 이 PC `PromptOnSecureDesktop=1`, `EnableLUA=1`, `ConsentPromptBehaviorAdmin=5` |
| E4 | 어떤 캡처 백엔드도 데스크톱을 전환하지 않는다 | **코드 확인** | 저장소 전체에서 `SetThreadDesktop`/`OpenInputDesktop` 히트 2곳뿐이며 둘 다 캡처 아님 (`native_video_host_main.cpp` 이름 probe, `secure_input_service_main.cpp` 입력 attach) |
| E5 | ACCESS_LOST 후 WGC 강등이 영구적이다 | **코드 확인** | `activeDesktopBackend = Wgc` 이후 복귀 경로가 명시적 클라이언트 요청뿐이고, 그 요청도 `resolve_output`에서 재실패 |
| E6 | SYSTEM 에이전트가 잘못된 세션을 타겟한다 | **코드 확인** | `WTSGetActiveConsoleSessionId()`가 유일한 세션 선택자. `docs/잠금화면_사전로그인_설계.md`의 실측 표에 호스트 세션 2 / 콘솔 세션 9 기록 |
| A1 | "데스크톱이 초당 33회만 갱신된다" | **가정 — 재검증 필요** | 측정 당시 RDP 연결 상태였고 Microsoft Remote Display Adapter가 **32Hz**. 33과 일치. 단 같은 조건에서 60fps 영상은 57~89/s를 기록해 하드 캡은 아님 → **P1로 확정 필요** |
| A2 | Winlogon에서 `GetDC(NULL)+BitBlt`가 동작한다 | **측정됨 — 참** | 2026-08-04 U1 probe. SYSTEM으로 Winlogon에 attach 후 **BitBlt 2720x1080(가상 화면 전체)과 DXGI 1920x1080 둘 다** UAC 창을 선명하게 캡처. `docs/잠금화면_사전로그인_설계.md`의 "DXGI만 가능" 주장은 **반증됨** |

> **A1이 P2의 규모를 좌우한다. 5분이면 된다. 먼저 한다.**

---

## 2) 작업 항목

규모: 소 = 반나절 이하, 중 = 1~2일, 대 = 3일 이상

### N. 도달성 (Networking)

- [x] **N1** 호스트 미디어 포트 후보 목록 + NAT 재작성 경고
  - 위치: `apps/native_poc/src/bind_port_candidates.hpp` (신규), `native_video_host_main.cpp`, `host_app_main.cpp`, `directory_client.{hpp,cpp}`
  - 규모: 소 · 선행: 없음
  - 완료조건:
    - [x] `--bind-port 443,3478,43000` 형태의 순서 있는 후보 목록 파싱
    - [x] 첫 후보 점유 시 다음 후보로 폴백, 실측 확인
    - [x] 단일 포트 지정 시 기존 동작 유지 (하위 호환)
    - [x] 관측 공인 포트 ≠ bind 포트일 때 `nat-port-rewritten` 경고 출력
    - [x] `remote60_bind_port_candidates_test` PASS
    - [ ] **회사 Wi-Fi 실기 접속 검증** ← 미완
- **N2** [취소] 릴레이 서버 — 2026-08-04 구현하지 않기로 결정
- **N3** [취소] 릴레이 클라이언트/호스트 통합 — 2026-08-04 구현하지 않기로 결정
  - 취소 사유: 서버 대역폭 과금, 추가 RTT, 그리고 평문 미디어가 서버를 통과하는 문제까지
    한꺼번에 떠안는 데 비해 지금 막힌 경로는 N1로 대부분 열린다. 필요해지면 되살린다.
- [ ] **N4** 미디어 암호화
  - 위치: `apps/native_poc/src/poc_protocol.hpp` (`kUdpFeatureEncryptedMedia` 비트만 있고 미구현)
  - 규모: 대 · 선행: 없음
  - 완료조건:
    - [ ] 미디어 페이로드가 경로상에서 평문으로 노출되지 않음
    - [ ] feature 협상으로 구버전 피어와 호환
  - 비고: N2/N3 취소로 "배포 전 필수" 조건은 사라졌다. 다만 공용 Wi-Fi 경유 트래픽이
    평문인 것은 그대로이므로 제품 항목으로 남긴다. 우선순위는 낮다.
- [ ] **N5** LAN 후보 주소 (같은 네트워크)
  - 위치: `directory_client.cpp` (하트비트에 사설 IP 추가), `apps/directory/server.js`, 클라이언트
  - 규모: 중 · 선행: 없음
  - 완료조건:
    - [ ] 호스트가 사설 IP를 함께 광고하고 디렉토리가 공인+사설을 함께 반환
    - [ ] 클라이언트가 두 후보를 동시에 시도해 먼저 응답하는 쪽 채택
    - [ ] 같은 네트워크에서 헤어핀 없이 접속
  - 비고: N3 취소로 레이스 구조를 재사용할 수 없게 되어 규모가 소 → 중으로 올라갔다.
- [ ] **N6** LAN 직접 다이얼 포트 유지 (43000 병행 listen)
  - 위치: `native_video_host_main.cpp` 핸드셰이크 루프 (`clientSock` 단일 소켓 전제)
  - 규모: 중 · 선행: N1 회사 검증 결과
  - 완료조건:
    - [ ] 디렉토리용 소켓(443)과 별개로 43000에도 bind
    - [ ] 두 소켓을 함께 대기하다 먼저 hello가 온 쪽을 미디어 소켓으로 채택
    - [ ] 디렉토리가 공개하는 주소는 여전히 443 소켓의 매핑
    - [ ] IP 직접 입력 접속이 43000으로 계속 동작
  - 비고: N1이 호스트를 43000에서 443으로 **옮기는** 변경이라, IP를 직접 입력해 붙던 LAN
    경로가 깨진다(클라이언트 기본값이 43000: `MainActivity.kt` 수동 입력, `GNLinkViewer --port`).
    디렉토리 경유는 `hostPublicUdpPort`를 받아 쓰므로 영향이 없다.
    N1이 회사망에서 실패하면 N6은 불필요해지므로 검증 후에 착수한다.

### U. 권한 / Secure Desktop

- [ ] **U0** 강등 고착 해제 + 상태 가시화
  - 위치: `native_video_host_main.cpp` (`dxgiFallbackRequested` 블록), `apps/host/src/realtime_runtime.cpp` (동일 로직 중복), `poc_protocol.hpp`, `native_video_client_main.cpp`
  - 규모: 소 · 선행: 없음 · **R3과 동일 코드**
  - 완료조건:
    - [ ] 강등 시 `requestedDesktopBackend`를 기억하고 데스크톱 복귀 시 원복
    - [ ] `ControlPongMessage`에 `secureDesktopActive` 추가, 클라이언트가 "보안 창이 떠 있어 표시할 수 없음" 안내
    - [ ] UAC 창 닫은 뒤 DXGI로 복귀함을 로그로 확인
  - 비고: **"UAC 한 번 뜨면 이후 계속 성능이 나쁜" 증상이 여기서 나온다.** 뒤 단계가 밀려도 단독으로 가치가 있다.
- [x] **U1** Winlogon 캡처 가능성 실험 — **2026-08-04 완료. 답: 둘 다 된다.**
  - 위치: `apps/native_poc/tools/winlogon_capture_probe.cpp` (타겟 `remote60_winlogon_capture_probe`)
  - 결과:
    - SYSTEM 프로세스가 `OpenInputDesktop`+`SetThreadDesktop`으로 Winlogon에 붙는 데 성공.
      접근이 거부된 적은 한 번도 없다.
    - **BitBlt: 2720x1080 — 가상 화면 전체(두 모니터)를 한 번에** 캡처. UAC 창 선명.
    - **DXGI: 1920x1080 — 출력 1개**만. UAC 창 선명.
    - `docs/잠금화면_사전로그인_설계.md`의 "secure desktop은 DXGI만 가능" 주장은 **반증됨.**
  - **U2b에 미치는 영향: BitBlt 채택.** 기존 `gdi_capture_worker_main.cpp`의 publish 루프를
    거의 그대로 재활용할 수 있어 `GNLinkInputService`에 d3d11/dxgi를 끌어올 필요가 없다.
    UAC 창은 정지 화면이라 낮은 고정 프레임레이트로 충분하다. 덤으로 BitBlt는 가상 화면을
    한 번에 잡아 **U4(멀티모니터)를 별도 작업 없이 흡수**한다.
  - probe는 U2b 착수 전까지 남긴다. 지금은 attach+capture 순서가 실제로 동작함이 증명된
    유일한 레퍼런스이므로, 구현이 안정될 때까지 지우지 않는다.
  - 다시 돌릴 때 주의 (probe가 이미 처리하지만 결과 해석에 필요):
    - 데스크톱 전환 직후 Winlogon은 아직 아무것도 그려지지 않은 검은 화면이다. 그때 찍으면
      "읽혔지만 비어있음"이 나오는데, 이는 접근 실패가 아니다.
    - `AcquireNextFrame`이 주는 커서 전용 프레임은 데스크톱 픽셀을 담지 않는다.
    - `control_*` 캡처가 실패하면 probe 자체가 고장난 것이므로 secure 결과는 무의미하다.
  - 비고: BitBlt가 되면 기존 GDI 워커 루프를 거의 재활용할 수 있다. 안 되면 `GNLinkInputService`에 d3d11/dxgi 의존성을 끌어와야 한다. **건너뛰면 며칠을 잘못된 구현에 쓴다.**
- [ ] **U2a** 세션 타겟을 요청자 기준으로 변경
  - 위치: `apps/native_poc/src/secure_input_service_main.cpp`
  - 규모: 소 · 선행: 없음 · **U2b보다 먼저**
  - 완료조건:
    - [ ] `WTSGetActiveConsoleSessionId()` 2곳을 `GetNamedPipeClientProcessId` + `ProcessIdToSessionId`로 교체
    - [ ] `RegisterServiceCtrlHandlerW` → `...Ex`로 올려 `SERVICE_CONTROL_SESSIONCHANGE` 수신
    - [ ] 콘솔 세션 타겟팅은 명시적 요청으로만 남김 (사전 로그인 기능 대비)
  - 비고: 먼저 하지 않으면 **"엉뚱한 화면을 완벽하게 스트리밍"** 하는 상태로 며칠을 잃는다.
- [ ] **U2b** SYSTEM 캡처 에이전트 (**BitBlt 경로 확정**)
  - 위치: `secure_input_service_main.cpp`에 `--capture-agent` 모드 추가 (별도 exe로 만들면 설치 파일 payload 표까지 손대야 함).
    캡처 루프는 `gdi_capture_worker_main.cpp`의 slot claim/BitBlt/sequence/SetEvent를 재활용.
    attach 순서는 `tools/winlogon_capture_probe.cpp`가 검증한 그대로.
  - 규모: **중** (U1 결과로 대 → 중. d3d11/dxgi 의존성이 불필요해짐)
  - 완료조건:
    - [ ] 매 publish tick마다 입력 데스크톱에 재attach 후 샘플링 (핸들 재사용 금지 — 전환 추종의 핵심)
    - [ ] 캡처 스레드는 매번 새로 만든다 (`SetThreadDesktop`은 창을 소유한 스레드를 거부)
    - [ ] 전환 직후의 빈 프레임을 전송하지 않는다 (Winlogon은 그려지기 전 검은 화면이다)
    - [ ] attach 상태/마지막 오류를 공유 헤더에 기록
    - [ ] UAC 창이 뷰어에 실제로 보임
- [ ] **U2c** 호스트측 프레임 수신 채널
  - 위치: `apps/native_poc/src/secure_capture_channel.{hpp,cpp}` (신규, `gdi_capture_process.cpp`에서 프로세스 생성부를 뺀 구조)
  - 규모: 중 · 선행: U2b
  - 완료조건:
    - [ ] 호스트가 파일 매핑/이벤트를 SYSTEM 접근 가능한 DACL로 생성하고 이름을 기존 단방향 파이프로 전달
    - [ ] 파이프는 단방향 유지 (보안 모델 변경 없음)
- [ ] **U2d** 프로토콜 확장 + 상태 반환 경로
  - 위치: `apps/native_poc/src/secure_input_protocol.hpp`, `secure_input_service_main.cpp`
  - 규모: 소 · 선행: U2b
  - 완료조건:
    - [ ] `BeginCapture`/`EndCapture` 종류 추가, 크기 검증 갱신
    - [ ] 에이전트 생존/attach된 데스크톱 이름/마지막 오류를 호스트가 읽을 수 있음
- [ ] **U3** 데스크톱 전환 중재
  - 위치: `native_video_host_main.cpp` 캡처 감독 루프
  - 규모: 대 · 선행: U2b, U2c
  - 완료조건:
    - [ ] 입력 데스크톱 전환 감지 시 인코더 소스를 secure 채널로 교체, 복귀 시 원복
    - [ ] 전환 시 키프레임 강제 + 클럭 앵커 리셋 (기존 폴백 경로와 동일하게)
- [ ] **U4** 멀티모니터 좌표 매핑
  - 위치: `secure_input_service_main.cpp`의 `map_point()`
  - 규모: 소 · 선행: U2a
  - 완료조건:
    - [ ] `SM_CXSCREEN` → `SM_XVIRTUALSCREEN`/`SM_CXVIRTUALSCREEN` 기반으로 전환
    - [ ] 보조 모니터의 UAC 창에 클릭이 도달
  - 비고: U2b가 BitBlt로 가상 화면 전체를 잡으므로 캡처 쪽은 이미 해결된다. 남는 것은
    입력 좌표를 같은 가상 화면 기준으로 맞추는 일뿐이라 U2b와 한 커밋으로 묶는 편이 낫다.
- [ ] **U5** 입력 전달 결과를 실제로 반환
  - 위치: `secure_input_broker.cpp`, `secure_input_service_main.cpp`, `poc_protocol.hpp`(`ControlInputAckMessage`), 클라이언트
  - 규모: 중 · 선행: U2d
  - 완료조건:
    - [ ] 주입 결과가 버려지지 않고 ack에 실려 돌아옴
    - [ ] 주입 실패가 클라이언트에서 실패로 보임
  - 비고: 지금은 `forward_to_agent`/`inject_message` 결과를 모두 버리고 무조건 ack한다. **실패가 성공으로 보고된다.**

### R. DXGI / RDP 복구

- [ ] **R1** ACCESS_LOST 복구: 어댑터 재선택
  - 위치: `apps/host/src/capture_backend_dxgi.cpp` (`resolve_output`, `recreate_duplication`)
  - 규모: 중 · 선행: 없음
  - 완료조건:
    - [ ] 재생성 시 어댑터를 재열거해 **현재 모니터를 소유한 어댑터** 위에 D3D 장치를 새로 생성
    - [ ] RDP 연결/해제를 넘겨도 DXGI 유지
    - [ ] 어댑터/출력 선택 로직을 순수 함수로 분리해 단위 테스트 (0크기 가상 어댑터 + 32Hz Remote Display + 물리 GPU 조합)
  - 비고: 현재는 기존 D3D 장치로만 재시도한다. RDP 중에는 데스크톱이 Microsoft Remote Display Adapter에 있어 물리 GPU 어댑터로는 **구조적으로** 찾을 수 없다.
- [ ] **R2** 유령 출력 배제
  - 위치: `capture_backend_dxgi.cpp` `resolve_output`
  - 규모: 소 · 선행: 없음 · **R1과 한 커밋**
  - 완료조건:
    - [ ] 크기 0/미부착 출력을 후보 선별 단계에서 건너뜀 (지금은 폴백으로 집은 뒤에야 `dxgi_output_size_invalid`로 실패)
    - [ ] Parsec/Virtual Display 어댑터가 있는 환경에서 정상 출력 선택
- [ ] **R3** WGC→DXGI 자동 복귀
  - 규모: 소 · **U0과 동일 코드 — U0에서 함께 처리**
- [ ] **R4** 호스트 런타임 폴백 로직 중복 제거
  - 위치: `native_video_host_main.cpp`와 `apps/host/src/realtime_runtime.cpp`
  - 규모: 중 · 선행: R1, R3
  - 완료조건:
    - [ ] 폴백/강등 로직이 한 곳에만 존재
  - 비고: 지금은 같은 로직이 두 런타임에 이중으로 있어 한쪽만 고치면 어긋난다.

### P. 성능

- [ ] **P1** RDP 해제 상태에서 캡처율 재측정
  - 규모: 소 · 선행: 없음 · **P2를 게이트한다**
  - 완료조건:
    - [ ] RDP 끊고 물리 화면(59Hz)에서 `dxgi-acquire acquires=` 1분 수집
    - [ ] 60 근처면 근거 대장 A1을 "기각"으로, 33 근처면 "확정"으로 갱신
  - 비고: A1이 기각되면 "60fps는 소스 한계라 불가능"이라는 기존 결론이 뒤집히고, P2보다 우선순위가 높은 작업이 생길 수 있다.
- [ ] **P2** NACK 재전송
  - 위치: 호스트 송신 링버퍼, 클라이언트 손실 검출, `UdpPacketKind::VideoNack`, feature 협상
  - 규모: 대 · 선행: P1
  - 완료조건:
    - [ ] 호스트 링버퍼 (8프레임 또는 2MB)
    - [ ] 클라이언트 손실 검출 (8~15ms 재정렬 유예, 프레임당 NACK 2회/32청크 상한)
    - [ ] `kUdpFeatureVideoNack` 협상
    - [ ] `kMaxConcurrentVideoAssemblies` 3 → 5~6 (60fps에서 3개는 약 50ms로 재전송 왕복에 부족)
    - [ ] `REMOTE60_NATIVE_UDP_SIM_DROP_PM` 10‰ 조건에서 `keyframe-request reason=2` 비율 80% 이상 감소, 지연 증가 없음

### V. 이월 항목 (구현계획_old.md에서)

대부분 코드가 아니라 **실기 검증** 과제다. 개별 규모는 작으나 장비/수동 조작이 필요하다.

- [ ] **V1** M1.6 window capture 검증 — H1/H2/H3로 코드는 대체 구현됨. 잔여는 A/B 검증
- [ ] **V2** D3D capture/scaler 경합 완화 interactive 검증 (`non-ShellWindow` 소스)
- [ ] **V3** M3.5 background 입력 주입 1차 수동 검증 (`DEL/SPACE/ENTER` 패치 반영 후)
- [ ] **V4** H264 안정성 Android 검증 (`:app:assembleDebug`, oversized input drop)
- [ ] **V5** 1080p60 동시부하 Gate A (`OSLink 동시 실행 DEC_AVG=50.11`, 목표 54)
- [ ] **V6** 보안 데스크톱 실기 검증 — **U 그룹에 흡수됨. U2b 완료 후 재정의**
- [ ] **V7** Android desktop-mode valid capture source verify (`GetShellWindow()` fallback 없는 환경)
- [ ] **V8** Android long-run soak (`targets → viewer → back` 10회 이상)
- [ ] **V9** Android client-instance exclude automation
- [ ] **V10** Android Phase F tap/drag + soft keyboard 실기 검증
- [ ] **V11** M8 색상 정확도 육안 검증 (컬러바 host 원본 vs Android 뷰어)
- [ ] **V12** M8 Android UI 실기기 검증
- [ ] **V13** M8 물리 디스플레이 세션 `GATE_A_DECODED_FPS_OK` 재측정 — **P1과 함께 수행 가능**
- [ ] **V14** 보류분: F1 실측, A1 2단계, A2, U2 잔여, S1, S2, G1 (사유/순서는 history 215)
- [ ] **V15** Windows Client stream-state 동기화 (P2 절전, 목록 화면 중 호스트 인코딩 정지)

### D. 기존 부채

- [ ] **D1** `remote60_gdi_capture_process_test` 성능 임계값 미달 (30fps vs 50 요구) — 이 단계 작업으로 생긴 것 아님
- [ ] **D2** `apps/client`, `apps/host` 레거시 타겟 빌드 불가 (`opus/opus.h` 없음) — 이 단계 작업으로 생긴 것 아님

---

## 3) 겹침 지도

한 덩어리로 처리해야 하는 것들. 따로 하면 같은 함수를 두 번 건드린다.

```
R3 (WGC→DXGI 복귀)  ≡  U0 (강등 고착 해제)       같은 코드
R1 (어댑터 재선택)   ≡  U3의 ACCESS_LOST 정책     같은 함수
R4 (런타임 중복)     ⊂  R1/R3 진행 시 반드시 마주침
V6 (보안 데스크톱)   ⊂  U2b
V13 (물리 세션 측정) ∥  P1 (동시 수행 가능)
```

**R1+R2+R3+R4 = U0 한 덩어리가 RDP 문제와 UAC 문제를 동시에 갚는다.**

---

## 4) 실행 순서

사용자 수동 확인이 필요한 항목은 뒤로 미루고 **혼자 끝낼 수 있는 코드 작업을 앞으로 당긴다.**
수동 확인은 한 번에 몰아서 요청한다.

### 트랙 A — 자율 진행 (코드 + 단위 테스트로 자체 검증 가능)

| 순서 | 항목 | 내용 | 규모 |
|---|---|---|---|
| A1 | **R1 + R2** | DXGI 어댑터 재선택 + 유령 출력 배제 (한 커밋) | 중 |
| A2 | **R3 = U0** | WGC 강등 고착 해제 + `secureDesktopActive` 상태 전달 | 소 |
| A3 | **R4** | 두 호스트 런타임의 폴백 로직 중복 제거 | 중 |
| A4 | **U2a** | 세션 타겟을 요청자 기준으로 (`WTSGetActiveConsoleSessionId` 제거) | 소 |
| A5 | **U4 + U5 + U2d** | 멀티모니터 좌표, 입력 결과 반환, 프로토콜 확장 | 중 |
| A6 | **N6** | 43000 병행 listen (트랙 B의 B1 결과가 통과일 때만) | 중 |

A1~A3은 RDP 문제와 UAC 문제의 **공통 기반**이라 따로 하면 같은 함수를 두 번 건드린다.
A4는 A5와 U2b보다 반드시 먼저다 — 안 하면 "엉뚱한 화면을 완벽하게 스트리밍"하게 된다.

### 트랙 B — 수동 확인 필요 (몰아서 요청)

| 순서 | 항목 | 필요한 것 | 게이트하는 작업 |
|---|---|---|---|
| B1 | **N1 최종 검증** | 회사 Wi-Fi에서 접속 시도 | N6, N4/N5 우선순위 |
| B2 | **P1 + V13** | RDP 끊고 물리 화면에서 1분 측정 | P2(NACK) 착수 여부 |
| B3 | **U1** | SYSTEM probe 실행 + UAC 창 띄운 상태 확인 | U2b 규모/방식 |
| B4 | **V 그룹** | Android 실기, 육안 비교 | 없음 (정리 성격) |

### 게이트 이후

| 조건 | 다음 작업 |
|---|---|
| B3에서 BitBlt 가능 | U2b/U2c → U3 (기존 GDI 워커 루프 재활용, 규모 축소) |
| B3에서 DXGI만 가능 | U2b/U2c → U3 (`GNLinkInputService`에 d3d11/dxgi 도입, 규모 증가) |
| B2에서 A1 기각 | P2보다 60fps 경로 재검토가 우선 |
| B2에서 A1 확정 | P2(NACK) 착수 |

**B1~B3은 합쳐서 한 시간이 안 걸리고 뒤 작업의 규모를 크게 바꾼다.** 트랙 A를 진행하는
동안 사용자가 편할 때 처리하면 된다.

---

## 5) 매 작업 후 기록 템플릿

- 변경 파일:
- 실행 명령:
- 로그 경로:
- 완료조건 대비 결과 (항목 ID별):
- 유의미성 판정: 개선 / 미미 / 무효 + 근거 수치
- 판정: 유지 / 롤백
- 다음 액션:
