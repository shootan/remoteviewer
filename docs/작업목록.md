# 작업 목록

> 2026-09-22 재작성. 기준 `main` = `a6cba13` (0.2.133). **남은 작업만** 적는다. 끝난 항목은 여기서 지우고
> `history.md` 에 남긴다. 새 작업은 생기는 즉시 한 줄로 추가하고, 우선순위가 높은 것을 위에 둔다.
> 상세: `구현계획.md`(항목) · `history.md`(기록) · `legacy/2026-09/`(끝난 조사·사건·설계 문서).
>
> 현재 배포: NAS `windows=0.2.133` · `android=0.2.20`(versionCode 19). 디렉터리 서버 배포본 = repo HEAD 에서
> `27d9e74` 하나만 뺀 것. Codex 검토를 거친 우선순위 근거는 `.claude/priority_2026-09-22.md`.

## 1. 실기·검증 대기 — 사용자 손 또는 현장이 필요한 것

| # | 항목 | 담당 · 사용자 손 | 판정 기준 |
|---|---|---|---|
| V1 | **승격 격리 실측 1회** — rollback 의 실행중/ACL 구분과 비파괴 보존 (`update_rollback_safety`, G gate 와 같은 방식) | 검증용 준비 → 사용자 관리자 실행 1회 | rc 0. 실행중 이미지는 bounded wait, ACL 거부는 즉시 중단·파일 무이동 |
| ~~V2~~ | ✅ **회사 PC2 → 0.2.134 완료**(2026-09-22 17:07). 인앱 0.2.130→0.2.134 완주, NAS `localVersion=0.2.134`. Defender 격리 재발 0(새 해시 우회 성공). 첫 시도는 구 0.2.130 업데이터의 abandon-race 로 실패 후 재시도 성공 — 이제 PC2 도 0.2.134 업데이터라 다음부터 안 남 | — | 완료 |
| V3 | **abandon-race 수정이 실행 주체로서 첫 시도 통과하는지** — PC2·집 호스트 모두 다음 버전을 0.2.134 업데이터로 받을 때 확인. 코드 서명 미도입이라 미래 버전 Defender 재오탐 가능성은 별개(C15) | 사용자 실기 → 로그 | 다음 인앱 업데이트 첫 시도 `Updated` |
| V4 | **0.2.130 라운드 실기 잔여** — ⑴ 폰 자동업데이트 폴백 경로(`self-update blocked by the device` + `handed the apk to the system installer` 두 줄이 있어야 폴백 검증) ⑵ 클립보드 호스트→뷰어 방향과 폰 두 방향·토글(뷰어→호스트는 09-22 PC2 로그 `[clipboard] update applied` 로 확인) ⑶ 툴바 두 버튼(데스크톱 캡처 전용) ⑷ 상향 끊김 뒤 세션 유지 | 사용자 | 각 항목의 로그 줄 |
| V5 | picker 가 **응답 없는 창을 건너뛰고** 나머지를 보여주는 실기 (0.2.128 썸네일 격리) | 사용자 | 응답 없는 창 옆에서 목록·세션 유지 |
| V6 | **HN13 현장 확인** — 혼잡 뒤 720p→1080p 복귀 (코드는 안정화 라운드에서 수정) | 사용자 | profile 복귀 시 encode 해상도 복귀 |
| V7 | 인앱 업데이트 잔여 확인 — 업데이터 `IsElevated=false`·integrity Medium (절차 `update_field_retest_runbook.md`) | 검증용 | 로그 |
| V8 | **방화벽 Block 규칙 6건 정리** (`temp\remote60_ctl_e2e_*`) — 승격 필요. 작업용은 손대지 말 것 | 사용자 승인 → 검증용 | 규칙 0건 |
| V9 | Android V1~V15 육안 검증 | 사용자 | — |
| V10 | **재발 시에만 다시 여는 묶음** (0.2.103~0.2.104 시절 실기 대기, 이후 빌드에서 매일 실사용 중): P9 UAC 클릭 좌표 · P11 UAC 복귀 혼잡 · P10 로그 401 · IME V2(한글 조합·한영 토글) · G7 영어 입력 · 09-08 17:15 물리 단절(양단 NIC 캡처) | — | 재발 보고가 있을 때 |

## 2. 코드 작업 — 체감 효과 순

| # | 작업 | 효과 | 규모 | 메모 |
|---|---|---|---|---|
| C1 | **같은 LAN 인데 릴레이로 붙는 문제** (회사 PC1↔PC2 가 NAS 릴레이로 스트리밍) | 지연·화질·NAS 대역 | 조사 소 + 수정 소~중 | LAN 후보가 지는 이유부터. 근거 `.claude/company_laptop_connect_2026-09-21.md`(미추적, R1 에서 history 로) |
| C2 | **접속 첫 시도 성공률** — F1 뷰어 Hello budget 을 하트비트 25s 위로 + "호스트 응답 대기" 표시 · F3 서버 wake 재송신. F2 keepalive · F5 유계 refresh 는 진단 로그 확인 뒤 | 접속 재클릭 소멸 | 소 | 대기 연장만을 원인 수정으로 삼지 않는다(Codex). 커밋 `e783733` 이 F1 을 후속으로 명시 |
| C3 | **뷰어측 ControlResume 송신** (item 8 layer 3) | peer-lost 30s 동안 입력 회복 | 중 | wire `9c5a858` · host `035cce7` 있음. 검증: 세션·세대·재전송/중복·인증/권한·실제 입력 복구 |
| C4 | **입력 ACK 직렬화 제거** (INPUT #2 = R02) — 마우스 move UDP latest-wins + 키 파이프라이닝 | 드래그·타이핑 지연의 뿌리(큐 대기 최대 252ms 실측) | 큼 | C1~C3 뒤. 설계는 Codex 확정(#351) |
| C5 | G5 마우스 커서 클라에서 따라 움직이게 | 조작감 | 중 | C4 와 같은 입력 경로 |
| C6 | NACK 대기·손실 지표 분리 (R07) + P2 NACK 재전송 | WAN 손실 시 회복 | 중~큼 | 네트워크 손실 / 의도적 생략 / 조립 만료 구분 |
| C7 | P8 텍스트 화질 (stable_text 튜닝 + QP 하향) | 글자 선명도 | 중 | |
| C8 | 제어 디스패처 liveness 감독 — 마지막 진행 시각 감시, 상한 초과 시 세션 회수 | 멈춤 2차 안전망 | 소~중 | 1차(썸네일 격리)는 0.2.128 완료 |
| C9 | 동시 뷰어 상태 독립성 (R12) — 프로세스별 세션 registry, 한 Host 다중 제어 정책 | 다중 세션 | 중 | |
| C10 | 원본 캡처 공급 계측 (R09) — 게임 FPS·포커스·정지 화면 구분 | 진단 | 중 | |
| C11 | K2 파일 전송 (K2a 하부 → K2b 뷰어 UI) | 기능 | 큼 | |
| C12 | G2 끊긴 세션 잠금해제 + G3 뷰어 버튼 UI (원장 D4 선행) | 기능 | 큼 | |
| C13 | 소형 묶음: 업로더 종료 크래시 0xC0000409(credentials 제거 뒤) · 업데이터 `result:` 줄 부재와 breakaway 성공 시 무기록(0.0.10) · 백업 이름에 releaseId(0.0.7) · ready 전 필수목록 검사(0.0.2) · REL-CRLF `.gitattributes`(D2 결정 뒤) | 낮음 | 각 소 | |
| C14 | 테스트 부채: Codex r9 비게이트 #5(childexit 파일 자기 run 만 회수) · #6(INCOMPLETE 계수) · 스위트 잔존 프로세스(0.0.6) · `udp_control_e2e` 상태 의존(0.0.5) · `gdi_capture_process` 문턱과 10배 격차(0.0.4) | 낮음 | 각 소 | |
| C15 | **코드 서명 (Authenticode) 도입** — exe 5종이 전부 미서명이라 새 버전마다 Defender 오탐(2026-09-22 PC2 0.2.133 staging 격리, ThreatID 2147731250). 인증서로 빌드 서명 단계 추가 | 새 버전마다 오탐·SmartScreen 경고 제거 | 중 (+비용) | 옵션: EV(즉시 평판, 하드웨어 토큰) / OV / Certum 저가 / 사설 PC 한정이면 self-signed. 비용 결정은 사용자 몫. 매니페스트 ECDSA 서명과는 별개 |

## 3. 배포·운영 결정

| # | 항목 | 조건 |
|---|---|---|
| D1 | **서버 `27d9e74` 배포** (store durable·인증 비동기 KDF). 배포본 = repo HEAD − 이 커밋 | 라이브 store 파일 스키마 검사 · 백업 · 기동 확인 뒤. journal 오류 0 · 재시작 0 (09-22 확인) |
| D2 | **REL-CRLF 결정** — `ui/shell.html` 바이트가 체크아웃 설정에 좌우됨 (0.2.129 CRLF 19807B / 0.2.130 LF 19339B) | ⑴ 그대로 두고 릴리스 간 해시 차이를 정상으로 기록 ⑵ `.gitattributes` 고정 후 재빌드. 사용자/Codex 결정 |
| D3 | **루트 워크트리 `D:\remote\remote` 를 main 으로** — 지금 `refactor/viewer-split`(main 보다 118 커밋 뒤). 거기 미커밋 docs 는 main 에 이미 커밋됨 | 미커밋·타 worktree 점검 뒤 별도 |

## 4. 기록 부채

| # | 항목 |
|---|---|
| R1 | `history.md`·`구현계획.md` 의 09-18~22 기록: item 8 layer 1·2, item 12 wake/relay(서버 배포 09-21 14:45), `e3897a1`, pc2-connect-diag(0.2.131), 헬스 게이트 D1/D2(0.2.132), abandon race r1~r10 + G 승격 실측(0.2.133). 근거 `.claude/*_2026-09-21.md`, `legacy/2026-09/updater_abandon_race_crash_2026-09-21.md` |
| R2 | 0.2.131/132/133 게시 근거 — 설치본·payload·manifest·NAS 해시 정합은 09-22 확인(`.claude/priority_2026-09-22.md`). 게시 로그 파일은 없음 |
| R3 | 133 실기 사실 — 09-22 13:14 readiness claim 완주, 호환행렬 row2(09-21 17:27)·row4(09-22 13:14) 실기 통과, 통과 경로도 로그 줄을 남김 |

## 5. 백로그 — 급하지 않음

- N4 미디어 암호화 · D3 encoder `set_d3d11_device` 라이브 재바인드(NV12 surface 모드만) · D4 unlock relay 원장 4건 · broker/worker 분리(DXGI wedge 무중단) · G4 60Hz 가상 디스플레이(최후수단) · G6 LDPlayer 무프레임 폴백 · A07~A12 · HN01/HN05/06/08 · SV01/02/06/07/AN01/I01 (원장 `legacy/2026-09/full_code_audit_2026-09-08.md`) · P3 데스크톱 60fps 원인 · 서버앱 갱신 자동화(의존 모듈·재시작 판단) · D1 GDI 캡처 임계 · D2 레거시 타겟 빌드
