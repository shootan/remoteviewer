# 작업 목록 (일정)

> 규칙: 모든 작업은 여기 한 줄로. 새 작업 생기면 즉시 추가. 완료는 `[x]`. 남은 작업 우선순위는 맨 위.
> 세부 근거는 `docs/구현계획.md`(항목 상세)와 `docs/history.md`(작업 기록) 참조.

## 🔴 남은 작업 — 우선순위 순

0.0.1 [ ] **업데이터 자신이 영영 갱신되지 않는다** — `GNLinkUpdater.exe` 는 manifest 에 있어 **staging 까지 받아 놓지만** `payloadNames`(`updater_effects.cpp:293-295`)에 없어 **설치 디렉터리로 옮겨지지 않는다**(`update_effects.cpp:468` swap 대상이 아님). 업데이터 버그를 고쳐도 배포되지 않는다는 뜻. 검증용 지적, **별건 유지 — 고치지 말 것**(history #500)
0.0.4 [ ] **`gdi_capture_process` 는 문턱이 관측 분산 안에 있다** (별건, 우선순위 인앱 업데이트 **아래**) — 상한은 화면 갱신률이 아니라 **복사 시간**이고(`1/copyAvg` 가 측정 fps 와 소수점까지 일치), 그 값이 **16.8~23.3ms** 로 움직이는데 **50fps 문턱은 정확히 20.0ms** 다. 그래서 실행마다 판정이 갈린다. 원인 가설 3개(live 경합·원인 미규명·Debug config)는 **전부 기각**됐다(6회 측정, 양쪽 config 전부 PASS). 별개 관찰: 같은 복사를 부모 1.8~2.5ms / 캡처 프로세스 17~23ms 로 **약 10배 격차**(history #506·#508)
0.0.7 [ ] **롤백 백업 이름이 고장난 문서를 가리킨다** (별건, **과거 잔재**, 서버 파일 손대지 말 것) — `manifest-backups/0.2.108`·`0.2.109` 는 **artifact 9개·Setup 없음**(swap 을 끝낼 수 없는 문서)이고, 쓸 수 있는 것은 `0.2.109-20260910T151705Z`(10개) **하나뿐**이다. 이름만 보고 고르면 **장애를 일으킨 그 문서를 복원한다.**
  - ⚠️ **업데이터 세대와 무관하게 그렇다**: 구 업데이터 필수 9개(정지 7 + ui 2, Setup 포함) → 맨 이름 둘은 Setup 이 없어 실패 · 신 업데이터(0.2.110) 필수 10개(Updater 추가) → 역시 타임스탬프본만 성공. **목록은 셋인데 선택지는 하나다.**
  - **진행형 결함은 아니다**: 다음 게시 때 백업되는 것은 0.2.110 pair(artifact 10)라 `0.2.110/` 이라는 맨 이름으로도 유효하고, **7번 게이트가 Setup 없는 manifest 를 애초에 못 나가게 막으므로 이 형태는 재발하지 않는다.** 그래서 우선순위는 인앱 건 아래.
  - 방향(계획 검토 후): 백업에 releaseId·payload 검사 통과 여부 표식을 남기거나 이름에 releaseId 를 넣기 (history #510)
0.0.5 [ ] **`udp_control_e2e` 가 상태에 따라 1P/17F 로 무너진다** (별건) — `udp hello ack failed`(`native_video_client_tcp_control.cpp:202`), 세션이 아예 성립하지 않는다. **Debug/Release 무관**, 같은 기계에서 몇 시간 전엔 18 PASS 였다. UDP 포트 점유가 후보지만 미확인 (history #509)
0.0.6 [ ] **스위트가 프로세스를 남긴다** (별건) — sweep 뒤 `viewer_udp_recovery` · `winhttp_tls_posture` 가 살아남아 Release 링크(`LNK1104`)를 막았다. 테스트가 자기 자식을 정리하지 않는다 (history #509)
0.0.2 [ ] **ready 전 필수목록 검사 누락** — 검증용 지적, 별건 (history #500)
0.0.3 [ ] **한글 UI 깨짐** — 검증용 지적, 별건 (history #500)

0.0 [~] **0.2.109 실기 대기** — 캐시 토큰 `/healthz` 광고(`422196b`) + 업로더 종료 계약(`b4d2a7a`) 반영본을 **NAS 게시 완료**(manifest `743fbb1f…`, 아티팩트 9/9 익명 https 검증). 배포는 이제 `automation/gnlink_deploy.sh` 로 한다(정책: `AGENTS.md` "배포 완료 기준", history #498). **복구 게시 완료**: `GNLinkSetup.exe` 가 manifest 에 빠져 swap 이 실패하던 것을 `releaseId=r-0.2.109-2`(version 0.2.109 유지)로 복구, 공개 URL **10/10 200**, 검증용 **구현 OK**(history #499·#500). **남은 것은 실기 하나** — **0.2.108 호스트에서 업데이트 재시도** → Host 가 `directory=online` 이 되는지. ⚠️ 캡처·인코더 의존 스위트 4개는 **콘솔 세션이 Disc 인 동안 관측한 red 라 판정 아님**(history #497), 재연결 후 재실행 필요

0. [~] **IME/입력+잠금해제** host-side IME **기본 활성화(V2)** — 0.2.91: 런처 env-unset→REMOTE60_HOST_IME=1, capability V2, 3상태 전환(EN-align 펌프+UI스레드 detach), 한/영·한자 make-only pulse, 공용 focus helper(GMux 창모드 입력), 호스트 초기 EN sync+target fence. **실기검증 대기**(한글 실시간조합/GMux영문/한영토글/초기EN). 잔여: 툴바 EN/KR/? 표시, GA_ROOTOWNER, P1b, WTS 실기, D4

1. [x] **WGCDEV/데스크톱 어댑터** 창모드 정상(0.2.82 실기 통과) + 데스크톱 정적멈춤=출력없는 어댑터 안착 문제였고 정상 어댑터(48969) 안착 시 영상/타이핑 정상 확인(#361)
1.1 [~] **P9 UAC 클릭 좌표 스케일 오차** — 호스트가 시작 시 1회 읽은 RDP 가상 디스플레이 rect(2236x1232)를 secure input 에이전트에 넘기고 갱신 안 함 → 캡처 (재)시작마다 실제 캡처 모니터 rect 재푸시 + 진단 예산 재충전 **코드 완료 (history #402, #404, #407, #409 선행 기록 1287883: versioned snapshot·단일 dispatch 경로)**, 0.2.103 설치본·UAC 실기 대기 (history #399, field_test_2026-09-07)
1.0 [~] **0.2.103 실기** — 09-08 정적 정지 없음·단일 모니터 UAC 클릭/복귀 부분 확인. 고화질 영상/재접속 정지 미해결. NAS 직접 분석·수정 필요 A01~A17: `docs/field_test_2026-09-08_0.2.103.md`, `docs/stabilization_audit_2026-09-08.md` (history #413). 제품 수정 미실행.
1.0.1 [~] **11:27 고화질 정지 패킷 캡처** — 1차 캡처 분석 완료·2차 캡처 방법 결정 대기 (history #415)
1.4 [x] **A01 업로더 401 busy loop** — 송신 가능할 때만 깨는 대기 조건·시간 기준 idle-alive·workerCycles, 회귀 [10] 수정 전 FAIL→후 PASS (history #416). 설치본은 A02 뒤
1.5 [x] **V14+A04 수신 recovery** — 루프 상단 공통 maintenance(5 ms rate-limit, 모든 경로) + 후속 없는 미완성 head 의 Codex 확정 포기 규칙(라운드 소진·replyAllowance·no-progress·terminalMin·hardCap)→IDR 경로, 회귀 S16~S23 수정 전 FAIL→후 PASS (history #417). A02 는 같은 task 진행 중
1.6 [x] **A02+A06+HN07 인코더 provenance** — 입력 FIFO 한 레코드 통합(빈 출력이 epoch 까지 pop), overflow 무효 래치·재동기(예산 3회/10 s·tick 재시도), unknown/future 출력만 체인 재닫힘, 회귀 [4] (history #418) — encode 실패 경로까지 배선(#420) + 동작 보존 추출·fake transform 실행 회귀(#423)
1.7 [x] **A04 후속(포기 identity tombstone + 포화 episode)** — 포기한 AU 의 늦은 청크가 head 를 재차단하던 결함 수정, 용량은 축출이 아니라 recovery episode 트리거(키 후보 K=2·floor·generation 판정 주입·수락된 키로만 해제), 포기 횟수 직접 단정·S24 (history #419·#421·#422)
1.8 [~] **0.2.104 실기** — A01(업로더 401)·V14/A04(수신 복구·포기 1회·포화 episode)·A02/A06/HN07(인코더 provenance) 반영 설치본 `dist/GNLinkSetup-0.2.104.exe`. 설치 후 401 뒤 업로드 재개·정적 꼬리 손실 복구·UAC 복귀 재동기·고화질 정지 재현 확인 (history #426). 11:27 물리 단절 해결 보장 아님 — 17:15~17:22 실기 증거 보전·문서화 완료 (history #428, `docs/field_test_2026-09-08_0.2.104.md`): 뷰어 자동 종료 6회(크래시 아님)·링크 무수신 7회·terminal 11회·복구 요청 4/실행 0. **원인 미확정**, 양단 NIC 캡처 필요 · **17:50~ 720p 고착 별건** 증거·코드 교차확인 완료 (history #431, 원장 HN13): profile 은 high 12Mbps 로 복귀했는데 encode 는 720p 고정 — `encodeSource` 오염 + aspectClose 교정 스킵. 수정 미위임
1.9 [~] **업데이트 기능** — **격리 상태에서 할 수 있는 구현은 전부 완료**(설계 `docs/업데이트_기능_설계.md`, history #427·#436~#449). 버전비교 공유벡터 · payload 이름 검증 · manifest 스키마 2(`artifacts[]`, 세 런타임 동일 한계) · HTTPS 정책 · 서명 검증 · 상태기계 + **Commit 지점** · 등록 공유단위(버전을 인자로) · **단일 릴리스 스냅샷 staging** · **실제 파일 회귀 4종** · **Relaunch/HealthCheck** · 클라 시작 시 비동기 확인. C++ 10종 **729 checks / 0 failed**. 기존 장애 (a)(b)(c) 는 설계·구현으로 해소(설치기 대체가 아니라 업데이터 경로에서). **배선 완료**(W1~W8, history #453~#459 — `run_update` 제품 호출자 1건, Android `MainActivity` 가 확인·다운로드·설치 호출). 배선 중 **제품 결함 3건** 발견·수정(호스트 미실행 기계에서 좋은 업데이트 롤백 / `validate()` 가 자기 갱신을 원천 봉쇄 / 재실행 결과가 호스트·클라 실패를 같은 값으로). **남은 것은 전부 실기**: `docs/수동확인_체크리스트.md` **UPD-FIELD-00~06**(셸 경유 비승격 실행 · UAC 0회/1회 · 실 HTTPS · 실제 등록 4단계 · `update_relaunch.cpp` 실행 실증 · UI 실동작). **버전 인상·설치본 생성은 사용자 승인 뒤 한 번에** — 그 전까지 라이브·배포·push 없음
1.3 [~] **P11 UAC 복귀 혼잡** — 복귀 flush 뒤 인코더가 쥔 전환 전 AU 가 오래된 stamp 로 송출돼 뷰어가 가짜 backlog 판정 → 호스트 epoch gate(새 epoch 첫 송출=IDR) + 뷰어 보유 재개 프레임 보정 **코드 완료 (history #405, #407: sender 펜스·gate fail-closed·뷰어 pending 수명)**, 0.2.103 설치본·UAC 실기 대기(S13 held-resume pending 수정 history #409); secure 중 kick 차단 보유는 별도 설계(뷰어 보정은 오탐 IDR 비용 감소일 뿐)
1.2 [~] **P10 로그 업로드 401** — 재등록 뒤 업로더 토큰 미갱신·서버 재시작 시 클라 세션 소멸로 NAS 로그 공백 → 업로더 재포인팅/401 일시정지/상한 재시도/owner epoch 펜싱 **코드 완료 (history #401, #403; 서버 세션 persist 는 범위 제외)**, 0.2.102 설치본·실기 대기(서버 배포 없음)
1.11 [x] **관측 엔드포인트 · https 전송 · 프록시 IP 오염** (history #482, 커밋 `2ff2fa6`~`b924286`) — 클라 전부가 관측 포트를 `http+1` 로 계산해 **TLS 뒤 443 이면 444** 로 보내고 있었다(호스트가 목록에 안 뜨고 뷰어가 릴레이 주소를 못 배움). 서버가 `observe:{port,host}` 를 광고하고 세 런타임이 **공유 벡터**(`apps/shared/observe_endpoint_vectors.txt`)로 합의. https 는 WinHTTP·http 는 기존 소켓(계약 동등성 회귀로 **실제 차이 2건** 발견: 응답 상한 3종 무음 절단 · 실패인데 status 200). 서버는 관측 없는 요청의 **소켓 주소 폴백을 삭제**하고 **409**(`observation_required`/`observation_expired`, 쓰기 이전 거부). 클라 재시도는 **1회**(카운트 회귀). Android 는 **모든 409 를 "PC 오프라인"** 으로 표시하던 결함 수정. origin 비교로 **후행 슬래시가 무인 PC 토큰을 날리던 것** 수정. ⚠️ **하드 의존**: https 전환은 nginx 설정만으로 안 되고 **서버 앱 + 양쪽 클라 업데이트**가 필요하다. ⚠️ **TLS 미검증 3건**: 1.2 런타임 증거 없음 · 강등 거부 실경로 미실행 · **유효 HTTPS 성공 대조 미실행**(NAS 준비 후 현장)

1.10 [x] **느슨한 포트 파싱 — `directory_client.cpp:451`** (Codex 승인 후 **좁게 수정 완료**, history #483) — UDP observe 응답의 `{"ip","port"}` 를 공용 `json_get_u32` 로 읽어 `observedPort_` 에 넣는다. 공용 리더는 정규식이 `([0-9]+)` + `regex_search` 라 **`29181.5` 에서 `29181` 을 집고**, 이 자리는 **상한 검사도 없어** `65537` 이 `static_cast<uint16_t>` 로 **`1` 로 절단**된다(`port == 0` 만 막는다). 이 값은 호스트가 광고할 공개 UDP 포트라 **도달성을 결정**하고, 틀리면 증상이 네트워크 장애처럼 보인다 — 바로 아래 `:458-460` 주석이 이미 *"the symptom on the client side is silence, which reads like a broken host"* 라고 적어 뒀다. **`:183` 의 observe metadata 에는 엄격 리더(`json_get_exact_u32`)를 적용했고(history #482), 논거가 같다: "지금 서버는 막지만 다른·구 서버는 안 막는다."** 참고로 `:870`(punch 대상)은 `port && port <= 65535` + `inet_pton` 가드가 있어 범위가 제한된다. **Codex 가 좁은 수정만 승인**해 처리했다: 1..65535 검사를 통과한 뒤에만 상태를 갱신하고, `fromDirectory` source 검증은 그대로 뒀다. **음성 대조로 결함을 재현**했다 — 수정 전 `65537`→`public=1.2.3.4:1`, `29181.5`→`29181`, `99999999999999`→`65535`, 빈 ip→`:29181`. 뷰어·폰이 공유하는 `directory_rendezvous.cpp` 의 파서도 같이 봤다(소수 절단 있었음, 상한은 이미 있었음).

2. [ ] **INPUT** 입력 ACK 직렬화 제거 (드래그·타이핑 느림의 진짜 뿌리) — P0 조인 텔레메트리(0.2.77) → P1 마우스move 비직렬화(UDP latest-wins) + 키 파이프라이닝. *Codex 교차검증 #351*
3. [ ] **NET** 펀치홀 / 직접 P2P — RTT·대역 병목 제거(입력 지연도 같이 줄임)
4. [ ] **G5** 마우스 커서 클라에서 따라 움직이게
5. [ ] **K2** RDP 없이 파일 전송 (K2a 하부 → K2b 뷰어 UI)
6. [ ] **PICK** 대상선택 picker 별도 창으로 (카드 안 뜸, flip-model 겹침)
7. [ ] **P8** 텍스트 화질 (stable_text 튜닝 + QP 하향)
8. [ ] **G7검증** 0.2.74 PC 영어 입력 실기 확인
9. [ ] **K1** 클립보드 텍스트 양방향 동기화
10. [ ] **G2** 끊긴 세션(RDP 끊김) 잠금해제 — 콘솔 세션 타겟 + 결과 ack
11. [ ] **G3** PC 뷰어 잠금해제 버튼 UI (G2 선행)
12. [ ] **SRV** 서버 로그 로테이션 10개 재배포 (코드 완료, 배포만)
13. [ ] **T1** 접속 시 클라 버전 전송·로깅
14. [ ] **G6** LDPlayer 창 무프레임 폴백 (렌더링모드로 회피 가능, 낮음)
15. [ ] **G4** 자체 60Hz 가상 디스플레이 — **최후수단**. 입력 수정(#2) 후에도 소스 합성이 막힐 때만. MTT pipe는 frame-pull 불가(control-plane only), DXGI는 change-driven이라 60Hz 모드≠60fps 캡처 (Codex #351)

## ✅ 완료 (이번 사이클)

- [x] **UACSLOW-2** UAC 뒤 잔여 지연(readback 100~500ms) 완화 (0.2.98→**0.2.99**: Codex 검증 P2 worker 계측 귀속 오류 수정, WARP 프로브 NOT REPRODUCED #389) — WGC 사용 뒤 AcquireNextFrame 내부 락 굶김 가설: DXGI acquire timeout 8ms + timeout 뒤 락 밖 2ms sleep(env A/B), 디바이스 보호 플래그·worker 락/호출 시간 계측(#387). 보호 OFF 복원은 Codex 반대로 미적용. **실기 확인 대기(호스트만 0.2.98)** — d3d-mt 로그로 가설 확정, 미해결 시 복제 전용 디바이스 분리
- [x] **UACSLOW** UAC 뒤 느려짐 근본수정 (0.2.97) — kick 이 "현재시각" stamp 로 들어간 뒤 느린 readback(100~300ms) 이 늦게 낸 실제 프레임이 그 뒤로 들어가 와이어 ts 역전 → 뷰어 stale-reference IDR 폭주. 호스트 stamp 단조 clamp + 코덱 AU/decoded synthetic provenance + 뷰어 synthetic 앵커 차단(#386). 회귀 PASS. **실기 확인 대기(호스트·회사 뷰어 둘 다 0.2.97)**. 후속: readback 지연 자체(GPU 경합, ~425ms 미계측 구간) 원인, note_packet synthetic recvGap

- [x] **STATICFRZ** 정적화면 타이핑 5초+ 멈춤 근본수정 (0.2.90) — 뷰어 stale-reference 오탐: 변경기반 느린 소스(200~400ms 간격)를 혼잡으로 오판해 매 프레임 디코더 리셋+키프레임 storm+델타 드랍(frameGapUs 1.2s). stale-behind-latest 판정을 조밀 도착일 때만 적용하도록 게이트(#379). 회귀테스트 PASS. **실기 확인 대기**
- [x] **G1/R1** 어댑터 이동 시 DXGI 장치 재생성 (0.2.70) — 실기 통과
- [x] **P4** ABR 정적화면 회복 (0.2.69)
- [x] **P5** 키프레임 폭주 감쇠 (0.2.69)
- [x] **P6** ABR 손실 반응 (0.2.69)
- [x] **P7** 혼잡붕괴 강등 + VBR 피크 200% (0.2.71), 오판 회귀 수정 (0.2.72)
- [x] **창모드** PC 뷰어 창 선택 복원 (0.2.73)
- [x] **G7** PC 영어 입력: 호스트 IME 중립화 (0.2.74) — 검증 대기

## 📦 백로그 (급하지 않음)

- [ ] **N4** 미디어 암호화
- [ ] **P2** NACK 재전송 (착수 게이트 충족됨)
- [ ] **P3** 데스크톱 60fps 진짜 원인 (Android/실네트워크)
- [ ] **U5** 입력 전달 결과 ack (G2에 흡수)
- [ ] **V1~V15** Android 실기 / 육안 검증
- [ ] **D3** (원장) encoder set_d3d11_device가 라이브 MFT 미재바인드 — NV12 surface 모드만 영향 (#357)
- [ ] **D1** GDI 캡처 테스트 임계 (RDP 32Hz 탓, 회귀 아님)
- [ ] **D2** 레거시 타겟 빌드 불가 (opus 없음)
