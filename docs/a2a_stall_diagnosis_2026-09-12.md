# UI 작업 반복 중단 조사 — 2026-09-12

사용자 범위: UI 작업 재개 금지. 두 Claude의 중단 원인 조사·수정만 수행.

## 직접 확인한 중단

- 작업용 원문 `.claude/ui_stall_worker_2123.md`: 마지막 제품 커밋 `f204916`
  (11:30:50), 마지막 도구는 검증용에 보낸 seq2120. 마지막 답변에 다음 UI 항목을
  진행한다고 썼지만 후속 도구 실행은 없었다. 권한·오류·명시적 대기 지시가 없었다.
- 검증용 원문 `.claude/ui_stall_verifier_2124.md`: 결과 수신 후 다음 실행을 연결하지
  않고 턴을 끝냈다. 작업용 실행 task는 생성하지 않고 메시지만 보냈다.
  “중복 task 금지”를 최초 위임 생략으로 해석한 것이다.
- 두 세션 모두 명시적 completed 갱신은 하지 않았다고 보고했다. 상위 UI task는
  생성 약 52초 뒤 completed가 되어 있었으며, 이것은 UI 완성 증거가 아니다.

직접 원인은 **미완료 실행을 남긴 채 두 담당자가 턴을 끝낸 것**이다. 런타임 결함이
이를 전부 설명한다고 단정하지 않는다. Codex 역시 다음 실행이 연결됐는지 확인하지
못한 책임이 있다. 이번 조사에서는 UI 수정·테스트·배포 재개를 지시하지 않았다.

## GMux 런타임 결함 및 수정

`C:/work/GMux/servers/agent-host/src/server.ts`와 실행 중 설치 번들의 source map을
대조해 다음 결함이 설치 코드에도 있음을 확인했다.

1. `result`를 작업 completed/failed로 자동 발행. 중간 보고·응답 없음도 완료가 된다.
2. 이전 턴 결과 처리 중 다음 task를 먼저 주입한 뒤 binding을 읽어, 다음 task를
   이전 결과로 완료·해제할 수 있다.
3. 큐를 한꺼번에 비우며 첫 assistant 프레임 전까지 running을 예약하지 않아,
   여러 입력을 연달아 주입·ack할 수 있다.
4. on-call 중 busy 때문에 대기한 메시지에도 off-call “사용자 승인 먼저” 문구를 붙인다.

GMux 커밋 **`d2c7fe0`**: 턴 알림은 일반 message로 분리(task 상태 변경 없음),
이전 binding을 먼저 분리·보고한 후 다음 입력 처리, 입력 전에 running 예약,
FIFO 매 항목 준비 상태 재검사, off-call/busy 원인 분리. 타임아웃·세션 종료도
상태를 임의로 실패 처리하지 않고 관측 알림만 보낸다.

## 검증과 한계

- 수정 전 신규 busy 승인 문구/FIFO 회귀 2건 실패, 수정 후 관련 4파일 **32/32 PASS**.
- TypeScript noEmit, 격리 esbuild 빌드 성공. 테스트는 공유 production helper와
  fake transport 기반 bridge 검사이며, 실제 Claude 세션 전체를 실행한 검증은 아니다.
- 격리 후보: `C:/work/GMux/.tmp-repro/stall-fix-candidate/index.js`, SHA256
  `9cc89e9deffd5b69999cbda24b4e643e48055d8de7a4a79de9d0f410be98bbbd`.
- **실행 중 GMux·설치 번들 미변경.** 적용에는 세션 보존 후 제어된 재시작이 필요하다.
  따라서 현재 실행 환경에서 재발이 없어졌다는 판정은 하지 않는다.
- 무조건 “계속”을 주입하는 watchdog·무한 자율 실행은 구현하지 않았다. 원래 작업의
  상태·범위·사용자 중지가 보존돼야 한다. 런타임 수정만으로 모델의 조기 턴 종료까지
  방지됐다고 보장하지 않는다.

## 운영 수정

AGENTS.md·CLAUDE.md에 최초 실행 task 위임과 중복 재촉을 구분하고, 자기 담당에
실행 가능한 일이 남았으면 다음 도구를 같은 턴에서 실행하도록 명시했다. 외부 대기는
실제 task ID·담당·반환 결과가 있어야 하며, 미래형 문장은 진행 증거가 아니다.
최종 연결 책임은 검증용·Codex에 있다. 사용자 중지 상태는 계속 유지한다.

다음: GMux 적용 시점 결정 → 격리 세션에서 A/B 연속 전달·명시 완료·busy 대기 확인.
GNLink UI 재개는 이 조사 완료 알림으로 자동 승인되지 않는다. push 없음.
