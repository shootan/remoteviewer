# GNLink UI — 화면 구성과 상태표

개선 작업의 **검토용 1차 산출물**이다. 큰 디자인 문서가 아니라, **어떤 화면이 어떤 상태를 가지며 그때
무엇이 보여야 하는가**만 적는다. 각 행은 나중에 **실렌더 스크린샷 한 장**으로 증명될 대상이다.

⚠️ 이 표는 **보이는 것**에 대한 계약이다. 내부 class/ID 전면 rename 은 범위가 아니다.

---

## 1. 화면 목록

| # | 화면 | 렌더러 | 파일 |
|---|---|---|---|
| S1 | 로그인 | WebView2 | `ui/shell.html` `#signInCard` |
| S2 | PC 목록 | WebView2 | `ui/shell.html` `#hostsCard` |
| S3 | 설정 | WebView2 | `ui/shell.html` `#settingsCard` |
| S4 | 업데이트 배너 | WebView2 | `ui/shell.html` `#updateBar` |
| S5 | 대상 선택 picker | **GDI** | `viewer_overlay_draw.cpp` |
| S6 | 세션 toolbar | **GDI** | `viewer_overlay_draw.cpp` |
| S7 | 창 제목 | Win32 | `viewer_window_proc.cpp:647` |

⚠️ **S1~S4 와 S5~S6 은 렌더러가 다르다.** 같은 디자인 언어를 쓰되 **증거의 종류가 다르고**, 보고에서
**DOM 하네스 / 제품 창 / OS 실기**를 구분해 적는다.

---

## 2. 확정된 문구 변경

| 위치 | 현행 | 변경 |
|---|---|---|
| 창 제목 (`viewer_window_proc.cpp:647`) | `remote60 native video client` | **`GNLink — <대상 PC명>`** |
| picker header (`viewer_overlay_draw.cpp:102`) | `Remote60` | **`공유 화면 선택`** |
| picker subheader (`native_video_client_shared_core.cpp:1285`) | `window_list_received count=N` | **사용자 상태 문구** (아래 S5 표) |
| picker 버튼 | `Refresh` · `Desktop` | **`새로 고침`** · **`전체 화면`** |
| picker 상태 | `Selecting target...` / `Waiting for first frame...` / `Connecting to host...` | **`선택하는 중…`** / **`첫 화면을 기다리는 중…`** / **`호스트에 연결하는 중…`** |
| picker 잠금 | `Target locked by host config` | **`호스트 설정으로 대상이 고정돼 있습니다`** |

⚠️ `window_list_received count=N` 은 **개발 로그 문자열이 사용자 화면에 그대로 나온 것**이다. 상태
문구와 로그 문구를 같은 필드로 나르던 것이 원인이므로, **로그에는 그대로 남기고 화면 문구만 분리**한다.

---

## 3. 상태표

### S1 로그인
| 상태 | 표시 |
|---|---|
| 초기 | 서버 주소(placeholder `https://주소`) · 아이디 · 비밀번호 · **로그인** 활성 |
| 복원됨 | 저장된 서버·아이디 채워짐, 비밀번호 칸에 focus |
| 로딩 | **로그인** 비활성 + spinner, 입력 비활성 |
| error | 붉은 메시지 1줄(서버가 준 문장), 입력 유지, 버튼 복귀 |

### S2 PC 목록
| 상태 | 표시 |
|---|---|
| 로딩 | spinner + `불러오는 중…` |
| 목록 | 온라인 먼저, 오프라인은 흐리게 + 연결 불가 표시 |
| **empty** | `등록된 PC가 없습니다` + `PC에서 GNLink 호스트를 실행해 주세요.` — **이미 구현돼 있다**(preview 확인) |
| error | 메시지 + **새로 고침** 버튼 |
| 연결 중 | 해당 카드만 진행 표시, 나머지 비활성 |

### S3 설정
| 상태 | 표시 |
|---|---|
| 초기 | 화질·fps·모니터 현재값 |
| 저장됨 | `다음 연결부터 적용됩니다` |
| error | 붉은 메시지, 값 유지 |

### S4 업데이트 배너 — **회귀 포함**
| 상태 | 표시 | 회귀 |
|---|---|---|
| 새 버전 없음/초기 | **숨김** | 🔴 `#updateBar.hidden` specificity (#523) |
| 상위 서명 버전 | 버전 문구 + **업데이트** + **나중에** | |
| 진행 중 | **업데이트** 비활성 + `준비 중…` | |
| 실패 | 버튼 **복귀**(죽은 채 두지 않음) + 오류 문구 | |
| `나중에` | 숨김, **로그인 화면 불변** | |
| 로그아웃 | 숨김 + 제안 회수 | |
| 401·검사 실패 | **표시 안 함** | `ShellUpdateNotice::show` |

### S5 대상 선택 picker
| 상태 | 표시 |
|---|---|
| 연결 중 | `호스트에 연결하는 중…`, 카드·버튼 비활성 |
| 로딩 | `화면 목록을 불러오는 중…` |
| 목록 | 균일 카드 · **썸네일 aspect 유지** · 이름 + 앱 아이콘 |
| **empty** | `공유할 수 있는 창이 없습니다` + 전체 화면 안내 |
| **error** | 사유 + **새로 고침** |
| 선택됨 | 해당 카드 강조(**색만이 아니라 테두리+체크**) |
| 선택 진행 | `선택하는 중…` → `첫 화면을 기다리는 중…`, 전체 잠금 |
| 잠금 | `호스트 설정으로 대상이 고정돼 있습니다` |

⚠️ **우상단 `Desktop` / `Desktop full-screen` 카드 중복 제거** → 선택 카드 한 곳. 전체화면/창 구분은
**실제 지원 목록 기준**으로만 노출한다.

### S6 세션 toolbar
| 항목 | 표시 | 규칙 |
|---|---|---|
| 대상 PC | 이름 | |
| 현재 화면 | 선택된 창/전체 화면 | |
| 경로 | **직접 연결 / 릴레이 / 연결 중 / 재연결 중** | 🔴 **badge + 아이콘 + 텍스트. 색만으로 구분 금지** |
| 통계 | fps·비트레이트 | 보조 상세. ⚠️ **정적 화면의 1fps 를 불량처럼 표시 금지** |

🔴 **경로 표시 규칙**: `ctx.session.relayPath` 등 **실상태**로만 판단한다. **unknown 이나 기본값
false 를 "직접 연결" 로 표시하지 않는다** — 모르면 **`확인 중`** 이다.
🔴 **auto-hide**: 원격 클릭을 가로채지 않고, **누르는 중에 사라지지 않는다.**
🔴 목록 앱의 **`… 창을 여는 중`** 이 연결 성립 후 **영구 잔존하지 않는다.**

### S7 창 제목
| 상태 | 표시 |
|---|---|
| 연결 전 | `GNLink` |
| 연결됨 | **`GNLink — <대상 PC명>`** |

---

## 4. 공통 규칙

- **좁은 창**: 최소 폭에서 카드 1열, toolbar 항목 줄바꿈, 문구 말줄임.
- **고DPI**: **100 / 150 / 200%** 에서 잘림·겹침 없음. GDI 쪽은 `dpi_scale()` 경유.
- **다중 모니터**: 모니터 간 이동 시 DPI 재계산.
- **상태 구분은 색에만 의존하지 않는다** — 아이콘·텍스트·형태를 함께 쓴다.
- **키보드**: focus 링이 보이고 Tab 순서가 화면 순서와 같다.
- **hover / focus / disabled** 를 네 화면에서 같은 방식으로.

---

## 5. 🔴 기능 결함 — "대상 선택" 무반응 (원인 미확정)

사슬은 **이미 연결돼 있다**:
```
toolbar WM_LBUTTONUP → onTargets(viewer_startup)
  → set_picker_visible_and_sync_stream (viewer_picker.cpp:101)
  → release_swapchain / Invalidate
```
⚠️ **callback 이 있는 것을 확인하고도 handler 를 더 얹는 방식은 쓰지 않는다.** 아래 다섯을 **분리해
격리 재현**한 뒤에 고친다:

| # | 가설 | 계측 | 나오는 로그 |
|---|---|---|---|
| 1a | **down 이 기록되지 않음** | `WM_LBUTTONDOWN` 의 hit id | `[toolbar] down id=N x=.. y=..` |
| 1b | **누른 곳과 뗀 곳이 다름** | `WM_LBUTTONUP` 의 두 early return | `[toolbar] up ignored: no press recorded` / `... released over A but pressed B` |
| 1c | **callback 이 비어 있음** | 호출 직전 유무 | `[toolbar] targets clicked, callback present\|MISSING` |
| 2 | visible 전환이 안 됨 | `exchange` 로 전후를 한 번에 | `[picker] visible 0->1 activeGen=N` |
| 3 | **paint 가 안 옴** | 표시 1회당 첫 그리기에서만 | `[picker] first paint after show, client=WxH` |
| 4·5 | input gate / reveal 경합 | 2와 3 사이의 시간차 + `activeGen` | 위 두 줄의 시각 간격 |

⚠️ **이 계측은 아무 결정도 바꾸지 않는다.** 1b 의 두 갈래는 **원래 있던 `return 0`** 이고, 조용했을
뿐이다 — 정상 동작(버튼 밖에서 뗌)과 죽은 버튼이 밖에서 똑같이 보였다.

**읽는 법**: `down` 은 있는데 `up ignored` 면 **1b**(제품 결함 아님, 안내 문제). `targets clicked` 까지
가고 `[picker] visible` 이 없으면 **2**. `visible 0->1` 은 있는데 `first paint` 가 없으면 **3** —
swapchain 이 위에 합성되므로 **그리기가 안 온 것과 영상이 멈춘 것이 화면에서 구분되지 않는다.** 둘 다
있는데 화면이 그대로면 **4·5**.

**기대 동작**: 클릭 → **같은 세션에서** picker 표시 → 새 선택/닫기 후 **영상·입력 정상 복귀**. 실패 시
사유 안내. **새로 고침도 반응이 보여야 한다.**

⚠️ **picker 열기/닫기가 재접속 · 불필요한 capture 재시작 · IDR 폭주 · 입력 stuck 을 만들면 그 변경은
반려 대상이다.**

---

## 6. 건드리지 않는 것 (하드 경계)

codec · capture · readback · MFT · NACK · ABR · pacing · 와이어 ABI · 서버 auth/UDP/P2P 협상 ·
**업데이트 엔진**. 기존 렌더러·프레임워크 유지. **새 WebView 로 영상/picker 를 갈아엎지 않는다.**
UI 표시와 picker 입력/렌더 handoff 에 **필수인 최소 변경만** 한다.

---

## 7. 증거 계획

| 화면 | 증거 |
|---|---|
| S1~S4 | **실제 `shell.html` 을 WebView2 에 렌더한 PNG** (상태별 1장) + 클릭 → 메시지 송신 |
| S5~S6 | **제품 창 스크린샷** (GDI 라 DOM 하네스로 대체 불가) |
| S7 | 창 제목 문자열 실측 |
| 공통 | DPI 100/150/200% · 최소 폭 · 출하 UI **해시 일치** |

⚠️ **DOM 하네스 / 제품 창 / OS 실기를 구분해 적는다.** 하나가 다른 것의 증거가 되지 않는다.
⚠️ **iframe 그림만 보고 stream 안정성을 확정하지 않는다.**

---

## 8. preview 로 실제 드러난 것 (2026-09-11, 30장)

산출물: `.claude/ui-preview/` — `w520`(보통) · `w360`(좁음) · `w1040`(200% 배율 픽셀) × 상태 10.
도구: `remote60_ui_preview`(배포 `ui/shell.html` 을 실제 WebView2 에 렌더, 제품 메시지 빌더로 상태 주입).
⚠️ **한계**: 제품 창이 아니고, 실제 고DPI 모니터도 아니며, GDI picker/toolbar 는 포함되지 않는다.

**🔴 P1 — 좁은 창에서 업데이트 배너가 내용을 덮는다** (`w360-s4-update-*.png`)
배너가 `position: fixed` 인데 아래 카드에 상단 여백이 없다. 520px 에서는 배너가 2줄이라 겹치지 않지만
**360px 에서는 4줄이 되어 `GNLink` 로고를 가린다.** 같은 이유로 `나중에` 버튼이 오른쪽에서 잘린다.
→ 배너 높이만큼 본문을 밀어야 한다(고정 여백이 아니라 **실제 배너 높이 기준**).

**🔴 P2 — 연결 중인 대상이 어느 것인지 화면에 없다** (`*-s2-hosts-connecting.png`)
`사무실 PC 창을 여는 중` 이 **목록 아래 상태 줄 텍스트로만** 나온다. **어느 카드가 연결 중인지 카드에
표시가 없고**, 다른 카드도 눌리는 상태로 남는다. 검증용이 지적한 *"창을 여는 중 이 영구 잔존"* 과
같은 자리다 — 카드에 진행 표시를 주고, 끝나면 그 표시가 사라지는 것이 정상 복귀의 관측점이 된다.

**✅ 이미 괜찮은 것 (바꾸지 않는다)**
- **empty 상태가 이미 있다** — `등록된 PC가 없습니다` + 다음 행동 안내. 위 표의 *"현재 없음"* 은 제
  오기였고 정정했다.
- **온라인 표시가 색만이 아니다** — 초록 점 **+ `연결 가능`/`오프라인` 텍스트** + 오프라인은 흐리게.
- 로그인 placeholder 가 `https://주소` 로 바뀐 것이 화면에서 확인된다(#523).

---

## 9. 제품 창 촬영 — 되는 것과 안 되는 것 (2026-09-11 실측)

도구: `.claude/shoot_viewer.ps1` — **사용자 권한**, 새 승격 없음. 실행 중인 GNLink(Host 20012 ·
Stream 26104 · InputService 28832)를 **건드리지 않기 위해** 뷰어를 **아무도 듣지 않는 43999** 로 띄운다.
루프백이라 바깥으로 나가는 트래픽도 없다.

**✅ 얻은 것 — S7 창 제목**
실제 제품 창의 제목이 **`GNLink`** 로 확인됐다(`product-viewer-connecting-00.png` 의 타이틀 바).
`remote60 native video client` 는 사라졌다. **이건 제품 창에서 찍은 것이지 하네스가 아니다.**

**🔴 못 얻은 것 — 연결 전 화면이 머물지 않는다**
본문이 완전히 검은 채로 **약 400ms 만에 종료(exit code 5)** 했다. 즉 **`호스트에 연결하는 중…` 을
보여주며 기다리는 화면이 이 경로에는 없다.** 위 S5 표에 그 상태를 적어 뒀지만, **호스트가 응답하지
않을 때 실제로 보이는 것은 잠깐의 검은 창**이다.
⚠️ **한 번 검었다** 와 **계속 검다** 는 다른 주장이라 프레임을 연속으로 찍었고, 그래서 종료가 보였다.
⚠️ 실행 3회 중 2회는 내가 멈출 때까지 살아 있었다 — **종료 타이밍이 일정하지 않다.** 원인은 조사하지
않았다(범위 밖). 여기서는 **"연결 전 상태를 안정적으로 촬영할 수 없다"** 는 사실만 확정한다.

**따라서 남은 상태(목록·선택 진행·toolbar 경로 badge)는 살아 있는 세션이 필요하다.**
- ⚠️ **정정(Codex)**: *"fixture 호스트는 와이어를 타므로 금지"* 는 **과잉 제한이었다.** 금지 대상은
  **운영 세션 접촉과 프로토콜 변경**이지 **격리 실행 자체가 아니다.** 운영 세션을 건드리지 않고
  프로토콜을 바꾸지 않는 fixture 호스트는 **승인 범위 안**이다.
- ❌ 실 계정 로그인 — 비밀번호를 요구하지 않는다.
- ✅ **사용자가 평소 하는 원격 연결 중에 로그만 남기는 방식**(2단계 계측)이 남은 유일한 길이다.
  별도 조작을 요구하지 않는다.

---

## 10. 🔴 S5 개선안 — 안내 없는 검은 창 (표시안만, 구현 아님)

**현재**: 호스트가 응답하지 않으면 **제목만 `GNLink` 인 검은 창**이 뜨고 잠시 뒤 **조용히 사라진다**
(실측 exit code 5). **무엇이 왜 안 됐는지 한 글자도 없다.** 사용자는 "눌렀는데 아무 일도 없었다" 로
받는다 — 이번 task 의 *"실패 시 원인 안내"* 에 정확히 해당한다.
⚠️ **종료 타이밍 원인은 조사하지 않는다**(검증용 지시). 여기서 정하는 것은 **표시**뿐이다.

**표시안**

| 단계 | 표시 | 비고 |
|---|---|---|
| 시도 중 | `<대상 PC> 에 연결하는 중…` + 진행 표시 | 검은 화면 대신 **즉시** 나온다. 첫 프레임 전에도 |
| 지연 | `응답을 기다리는 중입니다…` | 몇 초 넘어가면 바뀌어 **멈춘 것이 아님**을 보인다 |
| 실패 | **사유 한 줄** + **[다시 시도] [닫기]** | 창이 **사라지지 않는다** — 사라지면 사유를 읽을 수 없다 |

**사유 문구**(가진 정보로 구분 가능한 것만; 없는 정보를 지어내지 않는다)

| 상황 | 문구 |
|---|---|
| 상대가 응답하지 않음 | `PC 가 응답하지 않습니다. 그 PC 에서 GNLink 가 실행 중인지 확인해 주세요.` |
| 주소는 닿았으나 거절 | `연결이 거절됐습니다. 계정이 같은지 확인해 주세요.` |
| 그 외 | `연결하지 못했습니다.` + **내부 코드 병기**(로그 대조용) |

⚠️ **색만으로 구분하지 않는다** — 아이콘 + 문구를 함께. 좁은 창에서 버튼이 줄바꿈되지 않게(P1 과 같은
실수를 반복하지 않는다).
⚠️ **재시도 버튼이 자동 재시도가 되면 안 된다** — 실패 로그가 덮인다.

**구현 전 확인 필요**: 창을 남기려면 종료 경로를 건드려야 하는데, 그 경로가 **왜 그 시점에 나가는지가
아직 미상**이다. 표시만 바꾸고 수명은 그대로 둘 수 있는지 먼저 봐야 하므로 **검토 후 구현 여부 결정**.

---

## 11. 1차(표시만) 결과 — **그 조건에서는 실행되지 않는 코드였다**

⚠️ **정정**: 처음에 *"수정이 증상을 못 고친다"* 로 적었는데 **틀린 결론이다.** 촬영 경로는
`connect()` 가 실패해 **`draw_overlay` 가 있는 present 루프에 도달하지 못한다.** 즉 그 환경은 이
수정을 **시험할 수 있는 환경이 아니었다.** *"수정이 안 통한다"* 와 *"이 조건에서는 그 코드가 실행되지
않는다"* 는 다르다 — 오늘 반복해서 틀린 바로 그 구분이다(검증용 지적).

`draw_overlay` 가 첫 프레임 전에 곧바로 반환하던 자리에 **`연결하는 중…`**(4초 넘어가면
**`응답을 기다리는 중입니다…`**)을 그리도록 넣었다. 조건은 `lastPresentedVersion == 0` —
`lastPresentedCaptureUs` 는 **picker 가 닫힐 때마다 0 으로 되돌리므로** 세션 중에 다시 0 이 되어
살아 있는 영상 위에 글씨를 쓰게 된다. 버전은 올라가기만 한다.

**그런데 제품 창은 여전히 검다.** 재촬영 결과:
```
window title: 'GNLink'
viewer exited after 120ms, code 5
wrote 1 frames
```
**창이 뜨고 120ms 안에 프로세스가 나간다.** `draw_overlay` 는 `viewer_present.cpp:380` 에서 확실히
호출되지만 **그 전에 WM_PAINT 가 한 번도 오지 않는다.** 찍힌 검은 화면은 *"early return 으로 아무것도
안 그린 화면"* 이 아니라 **아직 한 번도 그리지 않은 창**이다.

⚠️ **그러므로 이 수정은 신고된 증상을 고치지 못한다.** 원인은 `draw_overlay` 의 조기 반환이 아니라
**프로세스가 나가는 것**이다. 표시만 바꾸라는 범위 안에서는 여기까지가 한계다.
⚠️ 종료 타이밍은 **일정하지 않다** — 최근 3회는 120~400ms 에 exit 5, 그 전 2회는 2초 넘게 살아 있었다.
**원인은 조사하지 않았다**(지시 범위 밖).

**그럼에도 이 변경은 남긴다**: *"창은 떠 있는데 아직 프레임이 없는"* 상태는 실재하고, 그때는 이 문구가
나온다. 다만 **그 상태를 이 환경에서 만들 수 없어 제품 창으로는 미검증**이다 — fixture 호스트는
와이어 경로를 타므로 하지 않는다.

**2차(창 유지 + [다시 시도]/[닫기])가 실제 해법으로 보인다.** 다만 그것은 **종료 경로를 건드리는
일**이고, 왜 그 시점에 나가는지가 미상인 채로 손대면 안정성 경계에 닿는다 — 검증용 판단 대기.

---

## 12. 2차 — **연결 실패를 말하고, 읽을 시간을 준다** (완료)

**exit 5 는 미상이 아니었다.** `viewer_startup.cpp:390` 의 `connect()` 실패다 — 검증용이 소스에서
찾았고 나도 확인했다. 아무도 듣지 않는 43999 로 띄웠으니 **정상 동작**이었고, 종료 시각이 120ms~수 초로
들쭉날쭉했던 것도 **거부가 즉시냐 SYN 재시도가 있느냐**로 설명된다.
**문제는 종료 자체가 아니라, 이미 알고 있는 실패 사유를 한 글자도 말하지 않고 창을 닫은 것**이었다.
사유는 내내 `std::cerr` 로 나가고 있었다 — 사용자가 볼 수 없는 곳으로.

**한 것**: 실패 시 **창을 유지**하고 **사유 한 줄 + [다시 시도] [닫기]** 를 그린다.
```
127.0.0.1:43999 에 연결하지 못했습니다.
그 PC 에서 GNLink 가 실행 중인지 확인해 주세요.
(코드 5)
          [다시 시도]   [닫기]
```
- 사유는 **가진 정보로 구분되는 것만**: 주소 오류(4) · 연결 실패(5) · 준비 실패(3) · 그 외. **내부 코드
  병기**로 로그와 대조된다. 방화벽·계정 같은 **모르는 것은 말하지 않는다.**
- ⚠️ **자동 재시도 없음.** [다시 시도] 는 사용자가 누를 때만이고, 누르면 로그에 그렇게 남는다 —
  자동이면 실패 로그가 덮인다.
- ⚠️ **성공 경로 무접촉**: 연결이 되면 첫 바퀴에서 루프를 빠져나가며 **호출 순서가 이전과 같다.**
  `connect` 자체·와이어·협상·타임아웃 값은 건드리지 않았다.

**증거**
- 제품 창 스크린샷 `.claude/ui-preview/product-viewer-connecting-*.png` **30프레임**(이전엔 1프레임 뒤
  종료). 위 문구와 버튼 둘이 보인다.
- 회귀 `viewer_startup_failure_test` **4 PASS** — 포트가 실제로 비어 있음 · **2.5초 뒤에도 살아 있음**
  (이전 동작은 ~120ms 종료) · 사유를 띄울 창을 여전히 가짐 · **닫으면 종료되고 exit 5 는 그대로**.
- ⚠️ **버튼 클릭 자체는 회귀가 아니라 스크린샷 증거**다. 히트 테스트를 테스트에서 복제하면 레이아웃을
  두 곳에 적게 되므로 하지 않았다.

---

## 13. S1~S3 시각 점검 (2026-09-12)

**먼저 사진을 봤다.** 두 화면은 이미 같은 언어를 쓰고 있었다 — 같은 카드·모서리·여백, 같은 파란 강조,
같은 muted 색. **그래서 전면 재디자인을 하지 않았다.** 대신 사진이 실제로 드러낸 것만 고쳤다.

**🔴 슬라이더에 값이 없었다** (`w520-s3-settings.png` 전)
`최대 화질`·`초당 프레임` 옆의 숫자가 **빈 칸**이었다. 라벨을 쓰는 코드는 있었지만 **`restore` 메시지가
도착해야만** 실행됐다. 설정을 그 전에 열면 값 없는 슬라이더 두 개가 보인다.
→ 로드 시 한 번 라벨을 채운다. 이제 `12 Mbps` · `60 fps` 가 나온다.
⚠️ **preview 쪽도 같이 틀렸다** — 설정 화면을 `restore` 없이 찍고 있었다. 즉 **아무도 도달하지 않는
화면**을 사진으로 남기고 있었다. 이제 클라이언트가 하는 순서대로 `restore` 를 먼저 보낸다.
(P2 촬영에서 배운 것과 같은 실수다: **연출한 상태가 제품의 상태가 아니었다.**)

**키보드 포커스 표시가 없었다**
Tab 으로 이동하면 **아무 표시도 나타나지 않았다.** `:focus-visible` 로 강조 외곽선을 넣었다 —
마우스 클릭에는 나타나지 않으므로 필요한 사람에게만 보인다.
⚠️ **이건 스크린샷으로 증명할 수 없다**(캡처에 포커스 상태가 남지 않는다). **미촬영 항목으로 적는다.**

**바꾸지 않은 것**: 색 팔레트 · 카드 구조 · 문구 · 버튼 배치. 사진에서 문제가 보이지 않았고, 보이지
않는 것을 고치면 회귀만 생긴다.

---

## 14. 🔴 toolbar 경로 표시 — **모르는 것을 "직접" 이라고 말하고 있었다** (2026-09-12)

`client_session_toolbar.cpp:83` 이 이랬다:
```cpp
text += g.state.relay ? L" · 릴레이" : L" · 직접";
```
`relay` 는 **기본값 false 인 bool** 이다. 그래서 **아무것도 답하기 전** — 사용자가 바를 쳐다보는 바로
그 순간 — 화면은 **확정된 직접 연결**이라고 말했다. **기본값은 측정이 아니다.**

**세 번째 상태를 만들었다.** bool 하나가 세 답을 담을 수 없다.
| 상태 | 표시 |
|---|---|
| 아직 모름 | `◌ 경로 확인 중` |
| 직접 | `→ 직접` |
| 릴레이 | `⇄ 릴레이` |
기호를 붙인 것은 **색에 기대지 않고 셋을 구분**하기 위해서다(바는 한 줄 텍스트라 쓸 색도 없다).

**어디서 "알게" 되는가**
- 디렉터리가 답했을 때(`viewer_startup.cpp:339`) — 그때 `relayPathKnown=true`
- **주소를 직접 준 경로** — 그 길에는 릴레이가 없으므로 `직접` 이 **가정이 아니라 사실**이다.
  이전에는 이 경로에서 `relayPath` 를 **아무도 쓰지 않아** 기본값 false 가 그대로 표시됐다.

**단정은 제품 함수로 한다**: 문구 조립을 `session_toolbar_status_text(state)` 로 꺼냈고, 바가 그것을
호출한다. 테스트는 **규칙을 복제하지 않고 제품이 쓰는 그 함수**를 부른다.
`session_toolbar_click_test` **12 → 17 PASS**: 미확정이 `직접` 으로 안 나옴 · `확인 중` 이라고 말함 ·
릴레이/직접 각각 말함 · **셋이 텍스트로 서로 다름**.

⚠️ **미촬영**: 바의 실제 픽셀. 이 창은 테스트가 만들고 테스트와 함께 사라지므로, 화면을 남기려면
창을 테스트 밖까지 살려 둘 방법이 필요하다. **문구 계약은 위 단정으로 닫았고, 그림은 남지 않았다.**
⚠️ **auto-hide(원격 클릭 가로채기·누르는 중 사라짐)는 아직 손대지 않았다.**

---

## 15. picker·toolbar 를 세션 없이 그렸다 (2026-09-12) — **보자마자 영문 4곳이 나왔다**

`remote60_picker_preview`: `WindowPanel::ApplyWindowList()` 가 **평범한 구조체**를 받으므로 소켓도
협상도 없이 목록을 넣고, **제품이 paint 에서 부르는 그 `draw_overlay()`** 로 메모리 DC 에 그린다.
같은 도구가 툴바도 만들어 찍는다. **판정은 하지 않는다** — 그림만 만든다.

산출: `picker-connecting/empty/list/selected/locked/error.png` ·
`toolbar-path-unknown/direct/relay.png`

**🔴 소스만 보고 번역했을 때 놓친 영문 4곳** — 화면을 보자마자 드러났다
| 위치 | 전 | 후 |
|---|---|---|
| 카드 캡션 | `Desktop (full screen)` | `전체 화면` |
| 썸네일 자리 | `Loading preview...` | `미리보기 준비 중…` |
| 푸터 | `Connected` / `Input off` / `Rows .. (wheel to scroll)` | `연결됨` / `입력 꺼짐` / `.. 줄 (휠로 스크롤)` |
| 빈 목록 | `No shareable windows yet. Click Refresh.` | `공유할 수 있는 창이 없습니다. 새로 고침을 눌러 보세요.` |
⚠️ 앞선 라운드에서 *"picker 문구 한글화 완료"* 라고 적었는데 **소스를 훑어 바꾼 것이라 네 곳이 남아
있었다.** 화면을 안 보고 한 번역은 절반이었다.

**중복 표기 하나도 정리**: 전체 화면 카드는 **썸네일 자리와 캡션이 같은 두 단어를 두 번** 말하고
있었다. 썸네일 자리는 비웠다(캡션이 이미 말한다).

**toolbar 세 상태가 화면에서 구분된다** — `연결 중 · ◌ 경로 확인 중` / `연결됨 · → 직접` /
`연결됨 · ⇄ 릴레이 · 60fps`.

**아직 남은 것**(이 사진들로 보이지만 이번에 손대지 않음)
- **카드 아래 거대한 빈 공간** — 4개짜리 목록이 화면의 1/5 만 쓴다.
- **`전체 화면` 이 버튼과 카드 두 곳** — 중복 제거는 "대상 선택" 원인 확정 뒤(레이아웃·히트테스트가
  그 조사 대상과 겹친다).
- **썸네일 aspect 유지**는 실제 썸네일이 있어야 보인다 — `ctx.picker.thumbs` 에 넣어 볼 수 있으나
  이번 회차에는 하지 않았다.
- **auto-hide 거동**(원격 클릭 가로채기·누르는 중 사라짐)은 **먼저 관측**한 뒤에 판단한다.

---

## 16. 강조색이 두 개였다 (2026-09-12, 검증용이 사진에서 발견)

`shell.html` 쪽 네 화면은 **파랑**(`--accent #3b82f6`), picker 는 **초록**이었다. **한 제품의 두 화면이
자기 색을 다르게 알고 있었다.** 게다가 초록이 **같은 무게로 둘** — 선택된 카드와 `전체 화면` 버튼 —
이라 **무엇이 선택인지 색으로는 답이 안 나왔다.**

**고친 것**
- 버튼 active 채움 `RGB(48,96,62)` → **`RGB(59,130,246)`** (shell.html 과 같은 파랑)
- 선택 카드: 테두리 파랑 + 캡션 **옅은 파랑 틴트**(채움 아님) + **오른쪽 위 ✓ 배지**
  - 카드는 **상태**, 버튼은 **동작**이다. 둘 다 꽉 채우면 같은 종류로 읽힌다 — 그래서 카드는 틴트다.
  - ⚠️ **✓ 는 색 대신이 아니라 색과 함께다.** 흑백 스크린샷·색이 틀어진 모니터·파랑과 회청을 구분하지
    못하는 눈에서도 선택이 남아야 한다.

⚠️ **레이아웃 안전 조건 확인**: 카드 기하는 `compute_card_grid()` 하나에서 나오고 **그리기와 히트테스트가
같은 함수를 부른다**(`viewer_picker.cpp:72,202,216`). 그래서 기하를 바꾸면 둘이 자동으로 같이 움직인다.
`desktopButtonRect` 는 `compute_client_layout()` 소관이라 **이번 변경과 무관**하다.

**다음(아직 안 함)**: 카드 아래 빈 공간. 지금 격자는 **항목 수를 모른다** — `compute_card_grid_at()` 는
사각형과 DPI 만 받는다. 적게 있을 때 카드를 키우려면 **항목 수를 넘겨야** 하고, 그건 순수 함수의 서명
변경이다(호출 4곳, 전부 스냅샷을 갖고 있음). 안전하지만 별도 단위로 한다.

---

## 17. 카드가 공간을 쓰게 — 격자에 항목 수를 알려줬다 (2026-09-12)

**원인**: `compute_card_grid_at(RECT, dpi)` 는 **선호 카드 폭 하나로만** 배치를 정했다. 카드가 5개든
40개든 같은 크기라, 5개일 때 화면의 1/5 만 쓰고 나머지가 검었다. **"다섯 장" 과 "마흔 장 중 앞의
다섯 장" 을 구분할 정보가 함수에 없었다.**

**규칙**: 열 수를 1부터 올리며 **가장 큰 카드**부터 본다. 상한(420dpi)을 넘으면 건너뛰고, **선호 폭보다
작아지면 멈춘다.** 모든 행이 스크롤 없이 높이에 들어가는 첫 배치를 쓴다.
- ⚠️ **키우기만 한다, 줄이지 않는다.** 처음 구현은 30장을 9열로 **줄여서** 한 화면에 욱여넣었다 —
  그건 이 인자가 하려던 일이 아니고, 지금 붐비는 picker 의 모습을 바꿔 버린다. `break` 한 줄로 막았다.
- `itemCount == 0` 은 **"모른다"** 이고 **이전 배치를 그대로** 낸다. 기존 기대값이 전부 그 위에
  쓰여 있기 때문이다.

**🔴 테스트가 내 추론을 잡았다**
새 단정을 **규칙에서 유도해** 적었더니 30장 케이스가 **FAIL** 했다 — 내가 "선호 폭으로 되돌아간다" 고
썼는데 실제로는 줄이고 있었다. **출력을 베껴 기대값을 맞췄다면 그 결함이 사양이 됐을 것이다.**
(#520 의 규칙: 통과하는 테스트는 무엇을 옳다고 적어 뒀는지의 증거일 뿐이다.)

**경계 촬영**: `picker-count-01/04/12/30.png`. 4장(+전체 화면 = 5)은 **3열 2행**으로 커졌고, 30장은
선호 폭 그대로 스크롤한다.
⚠️ **안전 조건**: 그리기와 히트테스트가 **같은 `compute_card_grid()`** 를 부르므로 기하가 어긋날 통로가
구조적으로 없다. `desktopButtonRect`·`compute_client_layout()` 무접촉.

**부수 관찰**: 카드가 커지니 **`전체 화면` 이 버튼과 카드 두 곳에서 파랗게 겹치는 것**이 더 뚜렷해졌다.
중복 제거는 여전히 "대상 선택" 원인 확정 뒤다.

---

## 18. 썸네일 종횡비 — **결함 없음** (2026-09-12, v2 항목 1)

`ctx.picker.thumbs` 에 **모양이 한눈에 보이는 시험 이미지**를 주입했다. 단색이면 늘어나도 잘려도 같은
사각형이라 아무것도 알 수 없으므로, **테두리 + 대각선 + 가운데 정사각형**을 그렸다: 테두리 한 변이
없으면 잘린 것, 대각선이 모서리를 벗어나면 늘어난 것, 가운데가 직사각형이면 비율이 깨진 것이다.

세 모양(1920x1080 가로 · 720x1280 세로 · 900x900 정사각)을 `picker-thumbs.png` 로 찍었다.
**결과: 비율이 유지된다.** 가로는 카드를 채우고, 세로·정사각은 **좌우 여백으로 가운데 정렬**되며,
가운데 사각형이 셋 다 **사각형 그대로**다. `aspect_fit_rect()` 가 제 일을 한다.
덤으로 **BGRA 채널 순서도 맞다** — 파랑을 요청했더니 파랑이 나왔다(뒤바뀌었으면 주황이었을 것).
⚠️ **고칠 것이 없어 고치지 않았다.**

---

## 19. 🔴 오류 상태가 화면에 오지 않았다 (v2 항목 2에서 발견)

error 화면을 찍으려고 `SetStatus()` 에 오류 문구를 넣었더니 **화면에는 "공유할 수 있는 창이
없습니다" 가 떴다.** 오류가 아니라 **엉뚱한 사실**이다.

**원인**: 제품이 실제로 넣는 상태는 **개발 토큰**이다 —
`control_disconnected`(`viewer_control_client.cpp:440`) · `session_lost`
(`viewer_session_watchdog.cpp:88`) · `control_connect_failed`(`viewer_startup.cpp:676`) ·
`window_list_request pending` · `waiting_first_frame`.
- **전에는** 이 토큰들이 **그대로 화면에 그려졌다**(`window_list_received count=3` 과 같은 종류).
- **`displayStatus` 를 분리한 뒤에는** 토큰이 사라진 대신 **아무것도 채우지 않아**, 목록에서 유도한
  줄이 나왔다. 즉 **개발 문자열 노출**을 **틀린 정보 표시**로 바꾼 셈이다. 둘 다 틀렸다.

**고친 것**: 그리는 자리에서 토큰 → 한국어 문장으로 옮긴다(그 타깃만 `/utf-8` 이다).
| 토큰 | 화면 |
|---|---|
| `control_disconnected` | `호스트와의 연결이 끊겼습니다.` |
| `session_lost` | `세션이 끊겼습니다. 다시 연결해 주세요.` |
| `control_connect_failed` | `호스트에 연결하지 못했습니다.` |
| `waiting_control` · `window_list_request pending` | `화면 목록을 불러오는 중…` |
| `waiting_first_frame` | `첫 화면을 기다리는 중…` |
⚠️ **모르는 토큰은 그리지 않고** 목록 줄로 떨어진다 — 개발 문자열을 화면에 내보내지 않기 위해서다.

촬영: `picker-error-disconnected.png` · `picker-error-session-lost.png` ·
`picker-error-connect-failed.png` · `picker-loading.png`.
⚠️ 문구는 **제품이 실제로 넣는 토큰**으로 찍었다. 지어낸 문장을 `SetStatus` 에 넣어 찍으면
**아무도 도달하지 않는 화면**이 된다(설정 화면에서 같은 실수를 했다).

---

## 20. 명칭 통일 — 남은 것은 **하나**, 그리고 그냥 바꾸면 안 되는 것이었다 (v2 항목 3)

**전수 확인 결과**: 설치·등록·서비스·단축아이콘·창 제목·picker·toolbar·`shell.html` 의 사용자 표시
문자열은 **이미 전부 GNLink** 다(`serviceName=GNLinkSecureInput`, `productName`/
`clientShortcutName` 모두 GNLink). 남은 `Remote60...` 두 개는 **윈도 클래스 이름**(내부)이라 범위 밖.

**🔴 남은 하나**: `host_app_main.cpp:759` 의 `kRunValue = L"remote60"`.
이것은 **작업 관리자 → 시작 앱에 그대로 보이는 이름**이다. 사용자 표시 문자열이 맞다.

**그런데 이름만 바꾸면 더 나빠진다.** 기존 설치본은 레지스트리에 `remote60` 을 갖고 있다. 상수만
갈면 **옛 항목이 남아 부팅 때 계속 실행**되는데 앱은 `autostart_enabled()==false` 라고 보고하고,
나중에 "끄기" 를 눌러도 **새 이름만 지운다.** 즉 **끌 수 없는 자동 시작**이 된다.
→ **두 이름을 함께 다룬다**: 읽기는 **둘 중 하나라도 있으면 켜짐**, 켜기는 **새 이름 기록 + 옛 이름
삭제**, 끄기는 **둘 다 삭제**. 이름만 바꾸는 것보다 코드가 늘지만, 그게 이 변경을 안전하게 만든다.
⚠️ **레지스트리를 지금 건드리지 않았다.** 이 변경은 **사용자가 자동 시작을 토글할 때** 작동한다.

**부수 관찰(고치지 않음, 이 PC 상태)**: 현재 이 PC 의 Run 항목은
`remote60 → "D:
emote
emoteuild-local\...
emote60_host_app.exe" --tray` 로,
**설치본이 아니라 개발 빌드 경로**를 가리킨다. 사용자 기기 상태라 읽기만 했고 바꾸지 않았다.

---

## 21. toolbar auto-hide — **관측 결과 결함 없음** (v2 항목 4)

지시대로 **고치기 전에 봤다.**

**"누르는 중 사라짐"**: `collapse_toolbar()`(`client_session_toolbar.cpp:250`)에 이미
`if (g.menuOpen || g.pressed != kButtonNone || g.hovered != kButtonNone) return;` 가 있다.
주석을 믿지 않고 **실제 메시지로 시험**했다 — 버튼을 누른 채 마우스를 소환 구역 밖(`10,400`)으로
알려 collapse 타이머를 걸고 **1200ms**(지연 800ms보다 길게) 돌렸다. **바는 그대로 있었다.**
⚠️ 그것만으로는 *"아예 안 접히는 바"* 도 통과하므로 **반대쪽도 넣었다**: 누른 것도 hover 도 없을 때는
**실제로 접힌다**(1500ms 뒤 `IsWindowVisible == 0`). 두 단정이 함께 있어야 의미가 있다.

**"원격 클릭 가로채기"**: 접힌 동안은 `ShowWindow(SW_HIDE)` 라 **픽셀을 차지하지 않는다**
(`:52` 주석 그대로). 펼침은 상단 중앙 **250ms dwell** 로만 일어나고, dwell 타이머는 **움직일 때마다
재시작**되므로 지나가는 마우스가 바를 불러내 자기 경로에 놓는 일이 없다.

**결론: 고치지 않았다.** 두 우려 모두 이미 처리돼 있고, 없는 문제를 고치면 회귀만 생긴다.
`session_toolbar_click_test` **17 → 19 PASS**.

---

## 22. 🔴 "대상 선택" 사슬을 **호스트 없이 재현했다** — 가설 1·2·3 은 결함이 아니다 (v2.1 항목 1)

세션 로그를 기다리는 대신 조각을 이었다. `picker_open_chain_test`:
```
진짜 WM_LBUTTONDOWN/UP → 실제 툴바 창
  → onTargets(제품 배선 재현, 3줄)
     → set_picker_visible_and_sync_stream(ctx, true)   ← 제품 함수 그대로
        → picker.visible 전환 · swapchain 해제 · 툴바 내림
           → draw_overlay 가 그린다
```
**결과 11 PASS**:
- 가설 1 — 클릭이 콜백에 **도달한다**
- 가설 2 — `visible` 이 **실제로 뒤집힌다**
- 가설 3a — 보여진 picker 가 **클라이언트 영역의 100% 를 칠한다**
- 가설 3b — 닫힌 상태와 **다른 그림**이다(5.3%)
- 부수 — picker 가 뜨면 **툴바가 내려가고**, 닫으면 **돌아온다**
- **음성 대조** — `대상 선택` 배선이 없으면 **같은 클릭으로 아무것도 안 열린다**

**로그가 아니라 픽셀로 판정했다.** picker 의 `first paint` 줄은 stdout 으로 나가 이 프로세스가 잡을 수
없고, 무엇보다 *"그렸다고 로그가 말한다"* 와 *"화면에 있다"* 는 다른 주장이다.

**🔴 기대값을 두 번 틀렸고 두 번 다 코드가 아니라 내가 틀렸다**
1. *"보이면 픽셀의 절반 이상이 바뀐다"* → **4%**. 프레임이 하나도 없으면 **닫힌 상태도 창을 꽉 칠한다**
   (`연결하는 중…`). 두 그림 다 어두운 사각형이고 헤더·카드·푸터만 다르다. 주장을 **⑴ 창을 덮는가
   ⑵ 닫힌 상태와 다른가** 둘로 나눴다.
2. *"1280x800 의 95% 이상"* → **93%**. picker 는 `layout.clientRect` 를 칠하고 그건 제목 표시줄·테두리를
   뺀 **1264x760** 이다. 그리기의 빈틈이 아니라 **재는 자의 빈틈**이었다. 클라이언트 크기로 재니 **100%**.

**⚠️ 이 재현이 덮지 못하는 것 — 여기가 남은 경계다**
- **가설 5(reveal 경합)**: 제품에서는 **flip-model swapchain 이 GDI 위에 합성**되므로,
  `release_swapchain()` 과 표시의 순서가 어긋나면 **`draw_overlay` 가 돌아도 사용자는 멈춘 마지막
  프레임을 본다.** 이 테스트에는 **swapchain 자체가 없어** 그 경합을 만들 수 없다.
- **가설 1b**(버튼 밖에서 뗌): 여기서는 정확히 눌렀다 놓으므로 발생하지 않는다. 실사용 로그에서만 갈린다.
- **가설 4**(input gate): 실제 입력 라우팅이 붙은 세션이 필요하다.
- 제품의 3줄 배선(`viewer_startup.cpp:196`)은 **재현했지 실행하지 않았다.**

**즉 남은 후보는 1b · 4 · 5 로 좁혀졌다.** 1·2·3 을 다시 뒤지지 않는다.

---

## 23. Desktop 중복 제거 — 분리 가능해서 지금 했다 (v2.1 항목 3)

**먼저 현재 동작을 기록했다** (`viewer_window_proc.cpp:129,152`):
```
헤더 버튼   → begin_pc_target_selection(ctx, 0, "desktop_select_requested")
전체화면 카드 → try_hit_window_list_item → id 0 → begin_pc_target_selection(ctx, 0, "window_select_requested")
```
**같은 선택, 토큰만 다르다.** 게다가 강조색 통일 뒤에는 **둘 다 파랑**이라 *"어느 쪽이 선택인가"* 가
화면에서 더 애매해졌다.

**분리 가능 판단**: "대상 선택 무반응" 의 남은 후보는 **1b·4·5**(§22)로 좁혀졌고 **셋 다 이 rect 와
무관**하다. 결합은 **새로 고침이 이 버튼 기준으로 배치된다는 한 줄**뿐이었다.
→ 새로 고침을 **오른쪽 끝**에 직접 앵커하고 버튼을 없앴다. `desktopButtonRect` 는 **빈 rect 로 남겨**
기존 `point_in_rect` 가드가 **절대 맞지 않게** 했다 — 구조체에서 빼면 그 가드들을 동시에 고쳐야 하고,
그건 이번에 건드리지 않기로 한 자리다.

**단정 갱신은 유도해서 적었다**: `desktopButtonRect` 는 이제 **비어 있고**, 새로 고침은
`right == 1600-24`(오른쪽 여백), 폭 96·높이 30, 144dpi 에서 높이 60. 출력을 베껴 맞추지 않았다.
`viewer_layout_test` rc=0 · `picker_open_chain_test` 11 PASS · `viewer_picker_gesture_test` rc=0.
재촬영: `picker-count-04.png` — 우상단에 **`새로 고침` 하나**, 선택은 **카드의 ✓** 한 곳.

---

## 24. PC 목록·설정 시각 통일 — **렌더 근거로 닫음** (v2.1 항목 4)

재작업 없이 사진으로 확인했다(`w520-s2-hosts-list.png` · `w520-s3-settings.png`).
같은 카드 폭·모서리·여백, 같은 `.link` 스타일(`로그아웃`/`뒤로`), 같은 muted 색, 같은 파란 강조,
슬라이더 값 표시(`12 Mbps`/`60 fps`). **이미 하나의 언어다.**
- 목록의 **초록 점**은 강조색이 아니라 **온라인 상태 표시**이고 **`연결 가능`/`오프라인` 텍스트가
  함께** 있으므로 "색만으로 구분" 에 걸리지 않는다. picker 의 초록은 *강조색* 오용이라 다른 건이었다.

**한 가지만 어긋나 있었다 — 띄어쓰기.** 목록은 `새로고침`, picker 는 `새로 고침`.
Windows 표준 표기(`새로 고침`)로 페이지 쪽을 맞췄다. 같은 제품의 두 화면이 같은 동작을 다르게 부르지
않게 하는 것이 이 항목의 목적이다.

⚠️ `client_update_ui_test` 의 **해시 pin 은 FAIL** 이다 — 페이지가 바뀌었고 아직 게시 전이라
**정상 동작**이다. 게시 후 해소를 반드시 확인한다.

---

## 25. 🔴 "화면이 서로 다른 말을 한다" 를 쫓아가니 **셋이 나왔다** (검증용 지적, 2026-09-12)

지적: `picker-error-disconnected.png` 에서 헤더는 `호스트와의 연결이 끊겼습니다.`, 푸터는
`연결됨 입력 꺼짐`. 추정으로 맞추지 말고 **소스에서 확인**하라는 지시대로 세 곳을 읽었다.

**⑴ 내 사진이 틀렸다 — 제품은 모순되지 않는다**
`viewer_control_client.cpp:425` 가 `connected=false` 로 두고 **:440** 이 토큰을 넣는다.
`viewer_startup.cpp:674→676` 도 같다. 그런데 **내 preview 가 `connected=true` 인 채 토큰만 주입**해
**제품이 만들 수 없는 상태**를 찍었다. 설정 화면(`restore` 없이 촬영)·연결 중 카드(상태 주입으로 연출)에
이어 **세 번째 같은 실수**다. 각 shot 이 제품과 같은 플래그를 쓰도록 고쳤다.

**⑵ `session_lost` 는 진짜로 둘 다 참이다**
`viewer_session_watchdog.cpp:88` 은 토큰만 넣고 **`connected` 를 건드리지 않는다** — 제어 채널은
살아 있고 **미디어 세션만** 죽은 상태다. 모순이 아니라 **두 가지 다른 연결**이다. 사용자는 단어만
보고 그 둘을 구분할 수 없었다.
→ 푸터가 **무엇을 재는지 말하게** 했다: `연결됨/연결 끊김` → **`제어 연결됨`/`제어 끊김`**.
한 단어 늘려 **새 주장을 만들지 않고** 애매함만 없앤다.

**⑶ 그러자 진짜 제품 결함이 드러났다 — 오류 문구가 계속 가려지고 있었다**
고친 사진을 다시 찍으니 헤더가 **`호스트에 연결하는 중…`** 이었다. 아래 코드 때문이다:
```cpp
if (!ctx.control.connected.load(...)) statusLine = "호스트에 연결하는 중…";   // 무조건, 맨 마지막
```
- **방금 기록한 구체적 실패 사유를 덮어쓴다.**
- 끊긴 뒤에는 **아무것도 연결하고 있지 않다** — 채널이 사라진 것이다. 거짓 진행 표시다.
→ 없앴다. "연결하는 중" 은 이제 **대기 토큰(`waiting_control`)이 제어 채널이 아직 안 붙었을 때**만
나온다 — 그때가 그 말이 참인 유일한 순간이다. 연결 전 화면은 그대로 `호스트에 연결하는 중…` 이다
(`picker-connecting.png` 확인).

**결과**: 끊김 → 헤더 `호스트와의 연결이 끊겼습니다.` + 푸터 `제어 끊김`. **같은 말을 한다.**

---

## 26. 🔴 Codex NEEDS_CHANGES 3건 — 완료 철회 후 수정 (2026-09-12)

**⑴ 빈 목록 문구가 상태를 보지 않았다**
`viewer_overlay_draw.cpp:262` 는 `windowItems.empty()` 만 보고 **어떤 이유로 비었든 같은 말**을 그렸다.
`picker-error-connect-failed.png` 실측 화면:
```
헤더  호스트에 연결하지 못했습니다.
중앙  공유할 수 있는 창이 없습니다. 새로 고침을 눌러 보세요.   ← 다른 설명
버튼  새로 고침 (비활성)                                      ← 누를 수 없는 행동을 권유
```
**상충하는 두 안내 + 실행 불가한 지시.** "비었다" 는 그 자체로 아무 뜻이 없고, **뜻은 상태가 정한다.**
→ 헤더가 이미 사유를 말하는 상태(오류·로딩·미연결)에서는 **중앙에 아무것도 쓰지 않는다.** 진짜로
"연결돼 있는데 공유할 창이 없다" 일 때만 기존 안내를 낸다(`picker-empty.png` 확인, 새로 고침도 활성).
⚠️ 나는 같은 패턴을 `picker-error-disconnected.png` 에서 **보고도** 푸터만 고치고 중앙을 넘겼다.

**⑵ 자동 시작 마이그레이션이 쓰기 실패를 확인하지 않았다**
`host_app_main.cpp:797` 의 `RegSetValueExW` 반환값을 버리고 `:801` 에서 **무조건** legacy 를 지웠다.
쓰기가 실패하면 **정상 동작하던 항목만 사라지고 대체가 없다** — UI 에는 "켜짐" 인데 부팅 때 아무것도
뜨지 않는다. **성공했을 때만** 옮기고, 실패하면 **기존 항목을 그대로 두고 로그에 남긴다.**
⚠️ 이 함수는 **"개명만 하면 끌 수 없는 자동 시작이 된다"** 를 피하려고 쓴 것인데, **그 수정 안에 같은
종류의 구멍**을 남겼다.

**⑶ 내 문서가 지시보다 좁았다** — §9 의 *"fixture 는 와이어를 타므로 금지"* 는 과잉 제한이었고 위에서
정정했다. 금지는 **운영 세션 접촉·프로토콜 변경**이지 격리 실행이 아니다. → 대상 선택 **1b·4 는 자동
검증으로 닫을 수 있고**, **5 는 swapchain 이 있는 격리 제품 경로**를 준비할 수 있는지 조사 대상이다.

**⑷ 표현 정정**: 이번 회차를 *"UI 전면 개선 완료"* 로 말하지 않는다. **고친 항목**(오류 문구·연결 중
표시·강조색·격자·중복 제거·명칭·배너 겹침·슬라이더 값·포커스 링 정의)과 **확인 후 그대로 둔 항목**
(썸네일 종횡비·auto-hide·목록/설정 디자인)을 **구분해 적는다.** 후자는 개선이 아니라 **결함 없음 확인**이다.


## 27. 빈 목록 — 상태별 계약과 부정 대조 (2026-09-12, §26⑴ 후속)

검증용 v2.2 지시: **셋을 구분하고**, **전체 화면 카드의 노출·선택 가능 여부를 오류 상태 계약과
대조하고**, **구분 로직을 끄면 결함이 되돌아오는지** 확인할 것.

### 27.1 결정을 그리기에서 떼어냈다
`viewer_picker_empty_line.hpp` (순수 함수, 한글 없음 — 이 헤더를 `/utf-8` 없이 컴파일하는 타깃이
있으므로 문장은 그리는 자리에 둔다). 표로 검사할 수 있게 됐다 — `picker_empty_state_test` **19 checks**.

| 상태 | controlUp | 헤더가 설명 | 목록 수신 | 중앙 |
|---|---|---|---|---|
| 연결 실패 `control_connect_failed` | ✗ | ✓ | ✗ | **침묵** |
| 제어 끊김 `control_disconnected` | ✗ | ✓ | ✗ | **침묵** |
| 세션 끊김 `session_lost` | **✓** | ✓ | ✗ | **침묵** |
| 제어 대기 `waiting_control` | ✗ | ✓ | ✗ | **침묵** |
| 목록 요청 후 대기 | ✓ | ✓ | ✗ | **침묵** |
| **연결됨 · 목록 수신 · 빈 목록** | ✓ | ✗ | ✓ | `공유할 수 있는 창이 없습니다. 새로 고침을 눌러 보세요.` |
| 묵은 토큰 + 새 목록 수신 | ✓ | ✓ | ✓ | 같은 문구(**수신이 토큰을 이긴다**) |
| 대상 고정(잠금) | — | — | — | **침묵** |

⚠️ `session_lost` 는 **controlUp 이 살아 있는 오류**다(`viewer_session_watchdog.cpp:88` 이 control 을
건드리지 않는다). `!controlUp` 만 보는 검사는 이 줄을 놓친다.

🔴 **내가 §26 에서 쓴 잠금 문구 `호스트 설정으로 창 목록이 숨겨져 있습니다` 는 거짓이라 지웠다.**
호스트는 잠겨 있어도 목록을 **그대로 보낸다** — `host_control_session.cpp:88` 은 플래그만 세우고 바로
아래에서 `enumerate_shareable_windows()` 를 그대로 돌린다. 잠금은 **선택이 고정된 것**이지 목록이
숨겨진 것이 아니다. 그리고 그 사실은 헤더가 이미 말한다 → 중앙은 침묵.

### 27.2 카드가 그림만 활성이었다
`begin_pc_target_selection` 은 `!control.connected` 면 **선택을 거부**한다(`viewer_picker.cpp:155`).
그런데 카드는 `selectionLocked || selectionPending` 만 보고 그려져, **오류 화면에서 눌리지 않는 카드가
멀쩡해 보였다.** 같은 화면의 `새로 고침` 은 흐리게 나오는데 카드만 정상이었다.
→ 카드도 `actionsDisabled`(= `!connected || locked || pending`)를 쓴다. **계산해 두고 `(void)` 로
버리던 값**이다.

### 27.3 부정 대조 — 껐더니 되돌아온다
두 수정을 되돌린 빌드로 같은 상태를 다시 렌더해 **픽셀로 측정**했다.

| 상태 | 되돌린 빌드 중앙 문구 | 고친 빌드 | 되돌린 빌드 캡션 | 고친 빌드 |
|---|---|---|---|---|
| 연결 실패 | **y=273-283 에 나타남** | 없음 | 밝음(28px) | **흐림(28px)** |
| 제어 끊김 | **y=273-283 에 나타남** | 없음 | 밝음 | **흐림** |
| 목록 대기 | **y=273-283 에 나타남** | 없음 | 밝음 | 밝음(맞다 — controlUp) |

로직 단위에서도 **0.2.121 이 실제로 하던 판단**을 테스트 안에 그대로 두고 같은 표에 돌렸다 —
**9개 중 7개에서 불일치**한다. 되돌리면 표가 깨지고 대조는 통과하므로 **어느 쪽이 변화를 재고 있는지**가
드러난다. ⚠️ 실패하지 않는 부정 대조는 장식이다.

### 27.4 새 촬영
`picker-locked-empty.png` — **잠금 + 빈 목록**. 위의 거짓 문구가 나오던 바로 그 상태이고, 실제로
발생할 수 있다(설정으로 대상 고정 + 공유 가능한 창 없음). 현재: 헤더가 고정을 말하고, 카드는 흐리고,
중앙은 비어 있다.


## 28. 🔴 대상 선택 — 1b·4 를 닫고, **5 를 재현했다** (2026-09-12, §26⑶ 후속)

§9 의 과잉 제한을 정정한 뒤 **격리 실행으로** 남은 셋을 쳤다.

### 28.1 가설 1b (버튼 밖에서 뗌) — 닫음. **다만 진짜 결함이 여기 있었다**
로그 세 갈래는 이미 덮여 있었는데 **"사용자가 무엇을 보는가" 가 비어 있었다.** 1b 는 원래
*"제품 결함 아님, 안내 문제"* 로 적혀 있었으므로 정확히 그 안내를 쟀다 — `PrintWindow` 로 버튼을 찍어
색을 비교(`session_toolbar_click_test`, **26 passed**).

🔴 **누름이 화면에 전혀 나타나지 않았다.** 눌림 표시가 `pressed && hovered` 였는데,
`TrackMouseEvent` 가 **실제 커서 위치**를 보고 곧바로 `WM_MOUSELEAVE` 를 보내 `hovered` 를 지운다.
툴바는 **정지한 커서 아래로 스스로 나타나고**, 정지한 커서는 `WM_MOUSEMOVE` 를 만들지 않는다.
→ **눌림은 hover 가 아니라 press 를 따라간다**(`pressedOver`). 측정:

| 단계 | 버튼 색 |
|---|---|
| 쉼 | `1F252F` |
| 누름(커서 이동 없음) | **`181D25`** ← 예전엔 `1F252F` 그대로 |
| 누른 채 밖으로 끌기 | `1F252F` (되돌아감 = 지금 떼면 아무 일 없다는 신호) |
| 다시 안으로 | **`181D25`** |
| 밖에서 뗌 | 아무것도 실행되지 않음 + 쉼 색 |

### 28.2 가설 4 (input gate) — 닫음
제품 `WndProc` 을 **격리로 링크**해 진짜 마우스 메시지를 넣었다
(`viewer_window_proc_isolated_test`, **14 passed**). 게이트가 서면 **picker 클릭이 통째로 사라진다**
(선택 시작 0건, 호스트로 나간 입력 0건). 그러나:
- **300ms 창이고 latch 가 아니다** — 지나면 같은 클릭이 다시 동작한다.
- **무장하는 곳은 터치 포인터 경로 한 곳뿐**이다. 마우스 메시지는 절대 무장시키지 않는다
  → **마우스만 쓰는 세션에서는 가설 4 가 원인일 수 없다.**
🔧 다만 **삼킬 때 로그가 한 줄도 없었다** — 삼킨 클릭과 죽은 버튼이 똑같아 보인다. 창당 한 줄로
남긴다: `mouse down ignored: touch suppresses the mouse for another 299ms` + 삼킨 수 카운터.

### 28.3 🔴 가설 5 (reveal) — **재현했다. 순서 경합이 아니다.**
제품 present 경로(D3D11·MF)까지 링크하고 **합성 NV12 한 장**으로 swapchain 을 띄웠다 —
호스트도 디코더도 프로토콜도 없다. 콘솔 세션(`qwinsta` 활성), 이 PC.

```
프레임 표시 전에 picker        화면 0D0F14   ← 보인다
프레임 한 장 표시              화면 00E600
제품 호출로 picker 열기        화면 00E600   ← 그대로
                              창 자체 DC 0D0F14   ← picker 는 그려져 있다
                              ready=0, swapChain=null   ← 해제는 됐다
강제 repaint / SWP_FRAMECHANGED / 1px 리사이즈   전부 00E600
```

**그려졌는데 보이지 않는다.** 그리고 `release_swapchain()` 은 **불렸고 실제로 null 이다.**
즉 F-21 의 *"swapchain 을 놓으면 GDI 로 돌아온다"* 는 **이 환경에서 성립하지 않는다.** flip-model
swapchain 을 한 번 쓴 HWND 는 해제 뒤에도 DWM 이 **마지막 프레임을 계속 합성**한다.

**경계가 정확하다: 프레임이 한 장도 표시되지 않았으면 picker 가 보이고, 한 장이라도 표시된 뒤에는
보이지 않는다.** 보고된 증상이 **세션 중간**에만 나온 것과 일치하며, 가설 1·2·3 이 모두 깨끗했던
이유도 이것으로 설명된다 — **클릭도 전환도 그리기도 전부 정상이었다.**

**재현 방법** — 일회성 실험이 아니다:
```
cmake --build <build> --target remote60_viewer_window_proc_isolated_test --config Release
<build>/apps/native_poc/Release/remote60_viewer_window_proc_isolated_test.exe
```
`943672c` 의 CMake 가 `viewer_present.cpp`·`mf_h264_codec.cpp` 와
`d3d11 dxgi d3dcompiler mfplat mf mfuuid` 를 이 타깃에 링크한다 — **가설 4 하네스를 키운 것이라
새 파일로 보이지 않는다.** 14건 중 6건이 가설 5 이고, 단정은 **xfail 형태**(지금 제품이 하는 일을
기록해 재현이 조용히 멈추는 것을 막는다. 고치면 `is_green` → `!is_green` 으로 뒤집는다).

🔴 **그런데 그 파일 머리말이 *"이 하네스는 가설 5 를 말할 수 없다 — swapchain 이 없다"* 라고
적혀 있었다.** present 를 실제로 링크하기 **전에** 쓴 문장을 갱신하지 않았고, 검증용이 재현을
**눈앞에 두고 두 번 찾게** 만들었다. **증거에 붙은 낡은 표찰은 표찰이 없는 것보다 나쁘다.**

**독립 빌드 2회에서 재현**(작업용 `build-local` · 검증용 `build-verify`, 양쪽 다 콘솔 활성).
⚠️ **이것이 좁히는 것은 "한 빌드 트리의 산물이 아니다" 뿐이다.** 같은 PC·같은 GPU·같은 드라이버이므로
**기기 수는 여전히 1대다** — 빌드 2회는 기기 2대가 아니다.
🔴 **나는 이것을 "2대째" 로 읽을 뻔했다.** 느슨하게 읽을 여지가 있었고 **그쪽이 나에게 유리한
쪽이었다.** 검증용이 범위를 정확히 잘라 주지 않았으면 그 문장이 그대로 남았을 것이다 — **유리한
해석이 가능할 때가 가장 위험하다.**

⚠️ **아직 확인되지 않은 것**: 다른 GPU·드라이버·세션에서도 같은지, 사용자 실기에서 같은 경로인지.
이 PC 1대·콘솔 세션 조건의 측정이다.
🔴 **고치는 방법은 present 경로 설계 결정이라 여기서 손대지 않았다** — 후보(picker 를 swapchain 에
그리기 / 영상을 자식 창으로 분리 / bitblt 모델 복귀)는 성능·구조에 각각 값이 다르다. **Codex 확정
사항**으로 올린다.


## 29. 가설 5 수정 — picker 를 같은 swapchain 으로 present (2026-09-12, Codex 확정 ⒜)

### 29.1 관측이 계약으로 올라갔다
`DXGI_SWAP_EFFECT_FLIP_*` 문서: **flip present 가 한 번 성공한 HWND 에서는 GDI 가 화면에 닿지
않고, swapchain 을 파괴해도 그렇다.** §28.3 에서 측정한 것이 바로 이것이다 — 재현이 먼저였고 계약이
나중에 확인됐다.

🔴 **제품 주석 세 곳이 정반대로 적혀 있었고 전부 고쳤다.**
| 위치 | 적혀 있던 것 |
|---|---|
| `viewer_nv12_renderer.hpp:55` | *"Releasing the swapchain returns the window to ordinary GDI redirection"* |
| `viewer_picker.cpp:112` | *"Dropping the swapchain hands the window back to GDI"* |
| `viewer_startup.cpp:194` | *"its repaint is composited"* ← **고쳐지기 전에 고쳐졌다고 적혀 있었다** |
세 문장이 서로를 뒷받침해서 **틀린 전제가 세 곳에서 확인되는 것처럼 보였다.**

### 29.2 무엇을 바꿨나 (좁게)
- 영상은 **FLIP_DISCARD 유지**, 장치·디코더 공유 유지, NV12 렌더 경로 **무변경**.
- picker 는 **`draw_overlay` 한 줄도 고치지 않고** offscreen top-down BGRA DIB(`ctx.ui.picker*`)에
  그린 뒤, **같은 swapchain 에 불투명 프레임으로 present**(`present_picker_frame` ·
  `Nv12D3dRenderer::present_bgra`, 복사 셰이더 하나 추가).
- **picker 진입 시 `release_swapchain()` 을 부르지 않는다**(F-21 폐기). 효과가 없었고 복귀 때
  swapchain 재생성만 유발했다.
- 갱신은 **picker 가 다시 그려질 때만**. 영상 프레임은 picker 를 건드리지 않는다(측정으로 단정).
- **GDI fallback 을 깔지 않았다.** present 가 실패하면 **로그로 말한다** — 조용히 GDI 로 흘리면
  이 변경이 없애려는 증상(그려졌는데 안 보임)으로 정확히 되돌아간다.
- 어느 길로 갈지는 `gdi_reaches_the_screen()` 이 정한다 = **flip 모델 && 이미 present 했음**.
  둘 다여야 GDI 가 죽는다 — 구형 드라이버 fallback(`DISCARD`)은 GDI 가 살아 있고, 첫 present
  전에는 평범한 GDI 창이다.

### 29.3 검증에서 **새 결함이 하나 나왔다**
**리사이즈가 picker 를 다시 그리지 않았다.** 창 클래스에 `CS_HREDRAW/CS_VREDRAW` 가 없고 `WM_SIZE`
핸들러도 없어 `WM_WINDOWPOSCHANGED` 가 그대로 `DefWindowProc` 으로 간다. **GDI 시절에는 어차피
보이지 않아 드러나지 않았고**, present 로 바꾸자 **이전 크기가 늘어난 그림**으로 보인다(DXGI 가 마지막
프레임을 새 크기에 맞춰 늘린다). → picker 가 떠 있을 때만 invalidate.

### 29.4 실측 (`viewer_window_proc_isolated_test` **38 PASS rc=0**)
```
device=hardware   swapEffect=FLIP_DISCARD
첫 present 전       GDI 도달 O · 화면 0D0F14(picker)
영상 1장            화면 00E600 · 이후 GDI 도달 X ← 증상이 아니라 계약
대상 선택(실제 툴바) 화면 0D0F14 · 창 DC 0D0F14 ← 예전엔 이 둘이 어긋난 것이 결함이었다
                    picker present 1회 · 영상 present 0회
카드 클릭           선택 시작됨(그린 것과 히트테스트가 여전히 일치)
picker 닫기→영상    화면 00E600 · 왕복 3회 3/3
리사이즈            화면 picker 유지 · 표면 1464x780 = 클라이언트 1464x780
썸네일(가로·세로)    present 유지
영상 60프레임        60/60 · 그동안 picker 업로드 **0** · 711~1029 us/frame(1584x860)
연결·swapchain      제어 유지 · swapchain 한 번도 해제되지 않음
```

### 29.5 수정 전/후를 **한 바이너리에서** 보인다
"고쳤다" 는 **없앤 실패만큼만 값이 나간다.** 그래서 실패를 요청 시 재현한다 — picker 를 **창 DC 에
그리고 present 하지 않는 것**, 즉 제품이 예전에 하던 그대로:
```
BEFORE  GDI 로 그리고 present 안 함   화면 00E600(영상)   창 DC 0D0F14(picker)  ← 둘이 어긋난다
AFTER   제품 경로로 present            화면 0D0F14                              ← 00E600 -> 0D0F14
```
이 대조가 **화면을 바꿔 버리면** 이 변경의 전제가 틀린 것이고 수정도 불필요해진다. 커밋 사이를
오가지 않고 **한 실행에서** 짝이 보인다.

### 29.6 스트림을 멈추지 않는다 (재접속 없음)
**처음 열리는 picker 만** 스트림을 붙잡는다(`activeStreamGeneration == 0`). 세션 중간에 멈추면 매번
캡처가 내려가고 재접속·키프레임 폭주가 난다 — *"얼었나?"* 를 확인하는 동작이 얼음을 만드는 것.
generation 을 실제로 올린 뒤 대상 선택을 눌러 **스트림 정지 요청 0건**을 측정했다.

### 29.7 확인되지 않은 것
- **WARP 경로는 실행되지 않았다** — 하드웨어 장치가 만들어졌으므로 fallback 에 들어가지 않는다.
  WARP 에서의 동작은 **미측정**이다.
- **영상 경로 A/B 는 재지 않았다.** 그 경로의 변경은 성공 분기 안의 **스칼라 대입 2개**뿐이라
  이 규모에서 A/B 노이즈가 변경보다 크다. 위 수치는 **변경 후 값**이다.
- 🔴 **사용자가 보고한 원래 증상과 이 재현의 원인이 같다고 증명된 것은 아니다.** 모양이 같고 경계
  (세션 중간에만)가 맞을 뿐이다. 실기에서 확인되기 전까지는 **같다고 적지 않는다.**
- 이 PC 1대·콘솔 세션. 다른 GPU·드라이버는 미확인.
