# 17:15 이후 고화질 영상 반복 멈춤 — Codex 직접 조사

사용자 관측: 영상 재생 중 멈춤, 재접속하면1~2초 나오다가 다시 멈춤. Host 복구 기능이 작동해 잠깐 재개되는 것 같다고 추가 설명. 아래는 NAS 로그17:18경 사본 기준이며 이후 상태를 보증하지 않는다.

## 출처

원본은 `.claude/incident-1715-20260915/`에 읽기 전용 확보했다.

| 파일 | SHA256 |
|---|---|
| home-host.log | c429203180e026ad80215699f73f97cb2e7f8a88227e080d95e9b39c1cb94559 |
| office-viewer.log | 0ed8e76c451269ac26ec56b93e42a2bb9dbf6f02bf355b09cd918a2d62a1b032 |
| office-client.log | 0cbe15d51e0ddc4fcd38ed6ee51a6703e6cfe19f982ff03034c569fef4378452 |
| game-host.log | 26a3d0a977c38a24b1a2fea74f55176828c0652bae4e8f3675af23bc07d934d9 |

집Host f6e8e54a7d5aff7c / Stream27860(14:02:01 생성), 회사Viewer68f79d01. 회사Client는12,000kbps/60fps로17:16:00·24·32·48에 접속 시작. Viewer 버전0.2.126, Host의0.2.125 표찰은 알려진126패키지 REL-V01 맥락이며 그것만으로 설치버전을 확정하지 않는다. 게임Host는9/14로그뿐이라 이 사건에 섞지 않는다.

## 직접 확인한 흐름

| Viewer 세션 | 첫~마지막 새 프레임 표시 | 이후 |
|---|---|---|
| 61292-714270593 | 17:16:02.648(seq9941)~14.121(seq10137) | link-silent,17:16:20.873 Ping peer-lost,23.494 종료 |
| 60988-714293796 | 17:16:25.932(seq10754)~26.323(seq10792), 약0.391초 | link-silent,32.107 shutdown,32.115 종료. shutdown은 종료 과정 결과일 수 있어 최초 원인으로 삼지 않음 |
| 54108-714302640 | 17:16:35.304(seq11211)~35.641(seq11247), 약0.337초 | link-silent,42.364 Ping peer-lost,45.021에720p decoder 재생성,47.837 종료 |
| 38752-714318187 | 17:16:50.876~17:17:56.646 | 사본 끝까지 새 표시1066개/Pong67까지 진행. 그 뒤 지속 정상은 미확인 |

**Host는 Viewer의 새 프레임 표시가 멎은 뒤에도 같은 epoch에서 후속 프레임의 wire 로그를 계속 남긴다.**

- seq10137: Viewer17:16:14.121, Host17:16:18.729. 동일seq 대조로 벽시계 약4.6초 차이 확인; clock telemetry도 별도로 존재. 장치간 시각을 그대로 빼서 지연을 계산하지 않는다.
- 이후 Host는 동일epoch11에서10139/10141 등을 거쳐10738까지 송신 기록. 해당 구간에 새프로세스 기동 없음.
- 다른 실패세션도10792 뒤10793/10800,11247 뒤11250/11251 송신 기록이 있다. Host 통계 udpTxFail=0. **sendto/API 성공 기록이지 NIC 실제 송출이나 수신 도착의 증거는 아니다.**
- Viewer는 recv loop가 진행되는 동안 datagramAge가 증가하고 Pong도 끊긴다. 마지막 관측은 단순 화면 paint 지연만이 아니라 영상/제어 수신 공백이다. 실제 네트워크 단절인지 ingress worker가 수신/분배하지 못한 것인지는 미확정.
- seq10137이6.75초 뒤 다시 paint된 줄은 이미 표시한 동일 프레임의 재그리기다. 이를 새 프레임이 UI에서6.75초 기다린 증거로 사용하지 않는다.

## 사용자 복구 가설과의 대조

Host 프로세스는14:02생성 PID27860 그대로이며17:10이후 WerKernel/Operational 조회에서 새이벤트는 없었다. 새 GPU타임아웃/프로세스재시작을 확인한 것은 아니다(이벤트 부재로 모든GPU문제를 배제하지도 않음).

확인된 자동 반응은 ABR 하향이다. Host17:16:23.102에9Mbps,27.310에720p/6.6Mbps;45.102에9Mbps,49.236에720p/6.6Mbps로 내려간다. 후자는 벽시계 차이를 고려하면 Viewer54108의17:16:45.021 720p decoder 재생성과 맞물린다. **일부 데이터 재개와 일치하지만 실제 영상/제어 정상 복구를 증명하지 않는다.** 이후 통계에는 decodedFrames=0·skippedQueued=3도 있어 디코더 생성만을 영상복구로 세면 안 된다.

## 판정·다음 확인

이번 실패는13:47이후 첫 창목록에서 멎은 장시간 제어 차단과 다르다. 대상 선택/active=1/첫 영상까지 성공한 뒤 수신이 끊기며, Host는 송신을 계속한다. 현재 경계는 **Host 송신 API 이후~회사Viewer ingress 관측 전**이다. 네트워크/NAT/수신측 NIC·소켓·worker 중 정확한 차단점은 양단 패킷/수신스레드 근거 없이 단정하지 않는다.

원인 분리용으로 사용자에게 동일 영상4Mbps·30fps·약30초 시험 결과를 요청했다(아직 결과 대기). 설정은 자동 변경하지 않았다. 이 시험이 통과하더라도 고화질 문제 해결 또는 네트워크 원인 확정이라는 뜻은 아니다. 다음 필요 근거는 실패 시점의 양단 packet 도달/ingress 진행이며, 추가 캡처·앱조작은 이번에 실행하지 않았다.

미리보기 격리·skip 구현task t-8tlz9aam은 계속하되, 그 수정이나127업데이트가 이 별도 고화질 수신 단절을 해결한다고 주장하지 않는다. 재부팅·드라이버 변경·설치·서비스 재시작·push 없음.
