# 수동 확인 체크리스트 (Pending Only)

업데이트: 2026-09-08

원칙
- 자동/빌드/스크립트로 대체 가능한 항목은 제외한다.
- `사용자 확인` 또는 `실제 2PC/실제 앱 세션`이 필요한 항목만 유지한다.
- 완료 시 이 문서에서 체크하고, 상세 결과는 `docs/history.md`에 기록한다.

> ⚠️ **2026-09-08 정정.** 아래 UPD-FIELD 행들은 원래 "구현했으나 실기를 못 했다" 로 적혀 있었다.
> **사실이 아니었다.** `run_update` 의 제품 호출자가 0건이고, 업데이터 실행 파일 자체가 없으며,
> Android 는 파싱 코드에 호출자가 없다 — 즉 **아직 배선되지 않았다.** 두 가지는 성질이 전혀 다르다:
> 하나는 사람이 확인만 하면 되는 것이고, 다른 하나는 **아직 만들어야 하는 것**이다. 남은 배선은
> `docs/업데이트_배선_계획.md` 에 있다.
>
> 그래서 각 행에 **[배선 필요]** / **[구현됨·실기 대기]** 를 붙였다. **[배선 필요]** 인 항목은
> 배선이 끝나기 전에는 실기 자체가 불가능하다.

**UPD-FIELD-01~06 공통 전제**: 여섯 항목 모두 **설치본을 만들어 실제로 설치한 뒤에만** 확인할 수 있다. 지금 빌드한 바이너리를 그대로 실행하면 이 PC 에서 돌고 있는 **라이브 0.2.104 와 충돌한다.** 버전 인상·설치본 생성은 **사용자 승인 뒤 한 번에** 한다.

| ID | 우선순위 | 수동 확인 항목 | 실행/환경 | 통과 기준 | 출처 | 상태 |
|---|---|---|---|---|---|---|
| UPD-FIELD-01 | P0 | **[배선 필요]** **셸 경유 비승격 재실행** — 업데이트 뒤 `GNLinkClient.exe` 가 관리자가 아닌 보통 사용자 토큰으로 뜨는가 | 설치본 설치 후 실제 업데이트 1회. 작업 관리자 "권한 상승됨" 열 또는 Process Explorer 무결성 수준. **조건을 나눠 각각 1회**: ⓐ 평소 데스크톱 ⓑ **Explorer 가 죽어 있는 세션** ⓒ **다중 데스크톱/다중 세션** | ⓐ 클라이언트 무결성 수준이 **Medium**(**High 면 실패** — 승격 토큰을 물려받았다는 뜻). ⓑⓒ 셸 경로가 없으면 **클라이언트가 뜨지 않아야** 하고 **승격된 채로 뜨면 실패**. 그때 사용자에게 무엇으로 보이는지(창이 안 뜸/무반응/로그만)와 시작 메뉴로 직접 실행하면 정상인지까지 기록 | `09e6aee` (`update_relaunch.cpp` `launch_via_shell`), 설계 3.5·3.11, history #448 | [ ] |
| UPD-FIELD-02 | P0 | **[배선 필요]** **UAC 프롬프트 횟수** — 설계의 명시적 완료조건 | 호스트 트레이발 업데이트 1회, 클라이언트발 업데이트 1회 | **호스트발 0회**(이미 상승돼 있어 자식이 토큰 상속) / **클라발 정확히 1회**, 그리고 **취소하면 교체하지 않고 클라이언트를 계속 쓸 수 있어야** 함 | `09e6aee`·`c812dcd`, 설계 3.5·3.6, history #443 | [ ] |
| UPD-FIELD-03 | P1 | **[일부 배선 필요]** **실 HTTPS 왕복** — 지금까지 모든 fetch 가 주입 함수였음 | 실제 디렉터리 서버에 manifest 를 올리고 호스트/클라에서 확인 | manifest 를 실제로 받아 **서명 검증 통과**, 평문 리다이렉트 거부, URL 자격증명 거부 | `44e869c` (스키마 2)·`09e6aee`, 설계 4.1, history #446 | [ ] |
| UPD-FIELD-04 | P1 | **[배선 필요]** **실제 등록 4단계** — 지금까지 기록만 하는 ops 주입 | 실제 업데이트 1회 뒤 서비스·방화벽·시작메뉴·레지스트리 확인 | 서비스 이미지 경로 == 설치 경로, 방화벽 규칙 1건, `.lnk` 2개, `DisplayVersion` == **신버전** | `9ee9a3b` (Commit 지점)·`5045802`, 설계 3.6, history #445 | [ ] |
| UPD-FIELD-05 | P1 | **[배선 필요]** **`update_relaunch.cpp` 실행 실증** — 라이브를 건드리지 않으려고 **일부러 어디에도 링크하지 않은** 코드 | 실제 업데이트 1회 | `CreateProcessW`(호스트) · `StartServiceW`(입력 서비스) 가 실제로 동작하고, **교체 전에 돌던 구성과 같은 구성**이 돌아옴. `Stream/Capture/Viewer` 가 **중복으로 뜨지 않는지** 확인 | `09e6aee` (`update_relaunch.cpp` — **어디에도 링크되지 않은 채 커밋된 코드**), 설계 3.11, history #448 | [ ] |
| UPD-FIELD-06 | P2 | **[일부 배선 필요]** **UI 실동작** — 빌드본을 실행하면 라이브 0.2.104 와 충돌해 지금은 불가 | 설치본 설치 후 | 호스트 트레이 "Check for updates" 가 다섯 결과를 구분해 말함 · 클라 시작 시 확인이 **로그인 창을 막지 않음**(서버 불통 상태에서도 즉시 로그인 가능) · 새 버전 있을 때만 알림 | `09e6aee` (`client_shell_main.cpp` `start_update_check`), 설계 3.5·3.13, history #441·#448 | [ ] |
| M35-MANUAL-01 | P0 | Background 입력 주입 최종 확인 | 인터랙티브 데스크톱 세션에서 `powershell -NoProfile -ExecutionPolicy Bypass -File automation\\validate_background_input_injection.ps1 -DurationSec 20` 실행 | `INPUT_EVENTS>0`, `INPUT_NO_TARGET=0`, `INPUT_INJECT_FAIL=0`, 커서 비이동(수동), occluded 창 입력 반영(수동) | `docs/구현계획.md` M3.5, `docs/history.md` 87/88 | [ ] |
| CFG-2PC-01 | P1 | direct `--config` 2PC 스모크 | 실제 2PC host/client에서 앱 직접 실행(`--config`) | host/client 정상종료 + 게이트 로그가 wrapper 실행과 동등 | `docs/history.md` 85 (Next) | [ ] |
| FREEZE-REPRO-01 | P1 | callback stall 프리즈 재현 시나리오 최종 확인 | 사용자 재현 시나리오(프리즈가 과거 발생하던 동일 조건) | 3초급 프리즈 재발 없음, callback stall 미재현 | `docs/history_old.md` (\"final confirmation requires user-side repro scenario\") | [ ] |
| M9-2PC-GATE-01 | P2 | M9 외부 2PC Gate A/B 수락 판정 | 외부 2PC에서 `m9 off/on` A/B 실행 + summary 수집 | Gate A 판정 완료 + QoE/전환 동작 로그 확보 | `docs/history_old2.md` 70~74 (external 2PC pending) | [ ] |
| M13-EMU-REBIN-01 | P2 | M13 phase3 실제 에뮬레이터 rebind 검증 | LDPlayer/BlueStacks/Nox 실제 세션 | `capture-window rebound`, `capture-size-updated`, `hostCapProc/hostCapRebind` 확인 | `docs/history_old2.md` 79 (Next) | [ ] |
| M13-2PC-MODE-01 | P2 | M13 phase4 실제 2PC mode-switch UX 수락 | overview -> focus 전환을 실제 2PC에서 수행 | overview 저품질/저fps 확인, click-to-focus 지연/안정성 확인, metadata 정합 확인 | `docs/history_old2.md` 80/81 (Next) | [ ] |

## 업데이트 기능 실기 (UPD-FIELD-01~06) 에 대하여

**먼저, 이 절이 원래 하던 주장은 틀렸다.** 여기에는 "이 항목들은 자동 회귀로 대체할 수 없다" 고
적혀 있었고, 특히 `update_relaunch.cpp` 가 **어디에도 링크되지 않은 것**을 격리의 대가로 정당화하고
있었다. 그것은 격리 품질의 증거가 아니라 **그 코드가 한 번도 실행되지 않았다는 자백**이다.

`CreateProcess` 자체는 라이브 파괴가 아니다. **dummy exe · 임시 설치 root · 임시 로그 경로를
주입하면 같은 OS 실행 경로를 실제로 검증할 수 있다.** 지금 안 되는 이유는 실행 코드가 제품 이미지
이름을 스스로 정하기 때문이고, 그건 받아들일 제약이 아니라 고칠 설계다. 방법은
`docs/업데이트_배선_계획.md` §2 에 있다.

정정 뒤 남는 것은 훨씬 좁다:

- **UAC 프롬프트 횟수**(UPD-FIELD-02) 는 OS 동작이라 관측 외에 방법이 없다.
- **서비스 등록·시작의 성공 경로**(UPD-FIELD-04·05 일부) 는 관리자 권한이 필요하다. 실패 경로는
  회귀로 덮을 수 있다.
- **사람이 봐야 하는 것**(UPD-FIELD-06 의 일부) — 알림이 실제로 읽히는지, 로그인 창을 막지 않는지.
- **운영 인증서 체인**(UPD-FIELD-03 의 일부) — 격리 서버로는 정상 체인과 실패 경로까지만 덮인다.

나머지는 **회귀로 덮어야 하고, 덮을 수 있다.**

**모든 UPD-FIELD 항목의 선행 조건**: `docs/업데이트_배선_계획.md` §1 의 배선이 끝나야 한다. 그
전에는 설치본을 만들어도 **업데이트를 시작할 수 없다** — `run_update` 를 부르는 코드가 없다.
버전 인상·설치본 생성은 **사용자 승인 뒤 한 번에** 한다.
