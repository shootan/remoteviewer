# CLAUDE.md

이 문서가 **상세 정본**이다. `AGENTS.md` 는 상위 원칙·역할·완성 기준만 두고, 같은 내용을 두 곳에
적지 않는다.

## 최우선 실행·A2A·보고 규칙 (2026-09-11)

**이 절은 이 문서의 다른 운영 절차보다 우선한다.** 상위 안전 규칙과 사용자 지시는 유지한다.
목적은 메시지 형식 자체가 아니라 **불필요한 왕복·대기·재검사와 최종 안내 누락을 없애는 것**이다.

### 한 작업, 한 유효 지시서, 한 창구
- 최초 지시에는 **목표·범위·완료 기준·자율 수행 범위**를 정리해 한 번에 보낸다.
  변경은 조각 메모로 추가하지 않고 **이전 revision을 대체하는 통합본 + 변경 요약**으로 보낸다.
- 첫 줄은 **task-id / revision / kind / reply_to=seq**로 한다. 기존 task가 없으면 안정된 작업명을
  쓴다. 메시지마다 새 task를 만들지 않는다.
- 핵심은 **5~10줄 이내**로 전달하고, 길면 버전이 명시된 **정본 파일 하나**를 참조한다.
  길이를 맞추려고 필수 조건을 생략하지 않는다. 누적 append 파일에서 과거 지시를 추측하게 하지 않는다.
- 제품 작업은 **Codex → 검증용 → 작업용** 창구를 지킨다. 같은 범위의 구현·검사 왕복에
  Codex가 중복 지시를 보내지 않는다. 문서 전용 작업은 아래 2단계 예외를 쓴다.
- 검토 범위를 끝까지 읽고 지적을 **한 묶음**으로 보낸다. 같은 고정 후보에 대한 Codex·검증용
  지적은 가능하면 취합한다. 새 중대 결함을 숨기거나 위험을 방치하면서 한 라운드를 강제하지 않는다.

### 송신 뒤에는 중복 요청을 쌓지 않는다
- **최초 실행 위임을 생략하지 않는다.** 상위 task와 구분되는 작업용 실행 task를 한 번 생성하고
  상위 task에 그 ID·담당·완료 기준을 연결한다. 같은 범위의 재촉·수정은 기존 task를 참조한다.
  “중복 task 금지”는 이 최초 위임을 `a2a_send`만으로 대신하라는 뜻이 아니다.
- **delivered=1은 읽음·착수·완료의 증거가 아니다.** BUSY·queued·짧은 무응답만으로 실패나 미착수를
  단정하지 않는다. 세션·파일·실행 상태와 실질 응답으로 구분한다.
- **별도 ACK 전용 메시지는 기본적으로 생략**한다. 첫 실제 진행·검토·결과가 접수 확인을 겸한다.
- **한 task에 미결 상태 조회는 하나만** 둔다. 이미 물었다면 같은 지시·재촉을 다시 보내지 않는다.
  확인되지 않은 상태를 사용자에게 진행 중·완료라고 꾸미지도 않는다.
- 본문이 미리보기로만 보이면 **기존 원문의 파일 전달을 한 번만 요청**한다. 새 답변 재작성이나
  전체 내용의 반복 발송을 요구하지 않는다. 원문을 확보하면 같은 요청을 다시 하지 않는다.
- 늦은 메시지는 task/revision/응답 seq로 대조한다. 구 지시를 다시 실행하지 않고 필요하면
  **현재 revision 참조만 한 번** 알린다. 처리한 inbox 항목은 지원되는 resolve로 닫아 재주입을 막는다.
- **즉시 STOP 예외는 안전상 긴급 중지**다. 그 밖의 수정은 취합하고, 위험 없는 독립 작업은 계속한다.

### 턴 종료와 작업 종료를 분리한다 (2026-09-12 중단 재발)
- **자기 담당의 실행 가능한 일이 남았으면 같은 턴에서 다음 도구를 실행한다.** “다음으로 진행합니다”라는
  마지막 문장, 커밋, 진행 보고는 다음 실행이 아니다. 이것만 남기고 턴을 끝내지 않는다.
- 외부 결과가 실제로 필요하면 **기존 task ID·다음 담당·기다리는 결과**를 남긴다. 검증용은 작업용의
  결과를 받으면 검사 또는 같은 task의 보수 지시로 연결한다. 양쪽 모두 상대의 새 지시를 기다리는
  상태를 만들지 않는다. 연결 책임은 검증용, 최종 누락 확인 책임은 Codex다.
- **런타임 `result`는 한 턴 종료일 뿐이다.** 버스가 자동으로 completed/failed를 보내거나
  “응답 없음”을 표시해도 최종 근거를 대신하지 않는다. 명시적 task 상태 갱신과 결과 검토로 판정한다.
- 도구가 턴 종료 후 자동 재개를 지원하지 않으면 그 한계를 그대로 보고한다. 문구만으로 무한 자율
  실행을 약속하거나, 무조건 “계속”을 반복 주입하는 watchdog을 두지 않는다.
- 사용자 **중지·조사만** 지시는 별도 실행 장벽이다. 알림·지연 메시지·상태 복구는 재개 승인이 아니다.
  재개 여부를 확인하려고 중복 핑을 보내지 않는다.

### 최종 사용자 안내는 Codex가 책임진다
- 중간 A2A는 **실제 진척·결정 필요·정확한 blocker**만 1~3줄로 보낸다. 매 커밋·매 턴 완료 보고,
  장문 회고, 사소한 docs-only 추가의 최종 보고 재발행을 하지 않는다.
- **사용자 창구는 Codex 하나**다. 검증용은 Codex에 결과를 보내고 같은 보고를 사용자에게 중복
  발송하지 않는다. 사용자가 해당 Claude에게 직접 질문하거나 별도 역할을 지시한 경우는 예외다.
- 검증용은 **한 FINAL_PACKET**에 commit/버전·URL·산출물 해시·직접 검증/보고 인용 구분·미검증·
  사용자에게 필요한 동작을 묶는다. 문서 전용 작업에서는 자기 승인 대신 Codex의 검토를 요청한다.
- Codex는 검토 후 **해결 내용 / 반영 버전·위치 / 직접 확인한 사용자 행동 / 남은 미검증 /
  사용자가 지금 할 일 또는 없음**을 한 번에 최종 보고한다. 단순 A2A 수신 확인으로 대체하지 않는다.
- **에이전트 자동 작업 완료·사용자 UAC 실기 대기**와 **실기 성공**을 구분한다. UAC 대기를 이유로
  완료된 작업의 최종 안내를 누락하지 않는다. 자동 검증 가능한 필수 행동의 생략은 완료가 아니다.
- 별건·정책 task가 본래 Client/Host 작업의 최종 결과를 가리지 않게 구분한다.
  사용성·안전·버전·사용자 행동을 바꾸는 중요한 오류는 한 번 명확히 정정한다.

## 테스트 — 환경 의존성은 기록하고, 막힌 것만 막는다

**전면 금지는 없다.** 예전 규칙(*"RDP 접속 중이면 테스트를 진행하지 말고 사용자에게 종료를 요청한다,
RDP 상태의 결과는 PASS 든 FAIL 이든 판정 근거로 쓰지 않는다"*)은 **삭제됐다.** 그 규칙은 실제로는
디스플레이·캡처에만 해당하는 제약을 전체 테스트에 확대해, 아무 상관 없는 UI·로직 검사까지 몇 시간씩
세워 두었다.

- **세션 상태는 기록한다**: `qwinsta` 로 콘솔/RDP 를 확인해 **결과와 함께 남긴다.** 기록은 하되
  그것만으로 실행을 막지 않는다.
- **테스트마다 의존성 태그를 붙인다**: `display-capture`(화면 갱신률·해상도·모니터 토폴로지) ·
  `gpu`(DXGI·WGC·인코더) · `session-privilege`(콘솔 전용·승격·서비스) · `network` · `pure-logic`.
- **필요조건이 안 맞는 그 테스트만** invalid/skip 하고 **이유를 적는다.** 예: RDP 세션에서
  `gdi_capture_process` 는 `Microsoft Remote Display Adapter` 갱신률에 묶여 `fps >= 50` 을 만족할 수
  없었고(**이 PC 2026-08-29 실측 32Hz**), DXGI 도 `0x80070005` 로 막혀 WGC 폴백으로 떨어졌다.
  ⚠️ **이 두 숫자는 그 환경에서 측정된 사례이지 "모든 RDP 에서 항상 그렇다" 가 아니다.** 다른
  클라이언트·어댑터·설정에서는 다를 수 있으므로, **그때그때 측정해서** 판정한다.
- ⚠️ **역단정도 금지.** *"UI·프로세스 테스트는 환경과 항상 무관"* 도 규정하지 않는다. 근거 없이
  어느 쪽으로도 단정하지 말고, **그 테스트가 무엇에 의존하는지**로 판단한다.
- **실 Host 재기동 시험**은 환경·권한·캡처 범위별 한계를 각각 적는다. 전 조건을 한 덩어리로 묶어
  "RDP 라서 전부 불가" 로 처리하지 않는다.
- 사용자에게 **RDP 종료를 요청하는 것은 그 필수 검증만 남았을 때 1회**다. 자동화로 갈 수 있는 데까지
  간 뒤에 요청한다.

## 사용자 행동 우선 완료 조건

사용자가 실제로 하는 행동이 되지 않으면 완료가 아니다. 이 절은 2026-09-11 에 **업데이트 알림은
뜨는데 누를 버튼이 없는 릴리스**를 내보낸 뒤 생겼다.

- 작업을 시작할 때 **요청된 행동을 짧은 acceptance 로 먼저 적는다** — 실행 · 로그인 · 선택 ·
  업데이트 클릭 · 성공/실패/취소가 각각 어떻게 보여야 하는가.
- **UI 변경의 증거**는 이것이다: **렌더된 컨트롤의 가시성·활성·입력 가능** + **실제 입력** →
  **브리지/네이티브 처리** → **기대 결과**.
- ⚠️ 다음은 **증거로 치지 않는다**: 소스 grep · 수신 handler 존재 · 문구가 표시됨 ·
  테스트 PASS 총합 · 작업용 Claude 의 주장.
- **실제 제품 창 스크린샷**, 또는 **무엇이 제품과 같고 무엇이 다른지 설명된 product-equivalent
  test build 의 증거**가 있어야 한다.
- **셋을 구분해 적는다**: ⑴ DOM 하네스 ⑵ 제품 UI ⑶ OS 설치 실기. ⑴은 ⑵⑶의 증거가 아니다.
- critical 행동이 **미구현·미검증이면 구현 OK 도 일반 릴리스 완료도 하지 않는다.**
- 외부 장비가 필요한 검증만 남았으면 **"테스트 후보 · 실기 대기"** 로 두고 완전 성공을 주장하지 않는다.

## 테스트 빌드와 출하 빌드는 달라도 된다

사용자 지시: *"테스트/실제 빌드 달라도 되잖아. 최대한 내 손 안 타게 테스트."*

- 같은 **제품 소스·UI·실제 처리 코드를 공유**한다면 시험 빌드에서 다음은 **허용**된다:
  manifest `asInvoker`/비관리자 실행 · 별도 app-id · 별도 userdata/install root · 별도 포트 ·
  test account · **test signer** · fixture endpoint · OS 효과 경계 주입.
- ⚠️ **금지**: 기능 규칙이나 수신 함수를 테스트에서 **복제**하거나 **항상 성공하는 stub** 로 바꾼 뒤
  *"production 경로를 끝까지 검증했다"* 고 주장하는 것.
- **build gate 로 검사한다**: 테스트 전용 기능·키·디버그 포트·SSL 우회·약화된 manifest 가
  **출하물에 없음**을 확인한다.
- ⚠️ **비관리자 test build 가 증명하지 못하는 것**: 실제 `requireAdministrator` · 토큰 승격 ·
  UIPI · UAC · SCM · Program Files 보호 · 실제 rollback 권한. 그 항목은 **별도 실동등 환경**에서
  본다. **테스트 빌드의 권한 완화는 실 UAC 검증을 대체하지 않는다.**
- 그 제약 때문에 **일반 Client/Host UI 개발·테스트 전체를 멈추지 않는다.**
- 운영 환경의 **UAC 자동 승인·보안 약화는 어떤 경우에도 하지 않는다.**

## 사용자 손을 최소화한다

사용자 지시: *"UAC 가 필요한 건 내가 실기 테스트 진행하고 그거 외에는 자동화해도 되잖아."*

- 테스트 실행 · 화면 캡처 · 버튼/화면 선택 · fixture 준비 · 로그 수집은 **에이전트가 먼저 자동화
  수단을 확인하고 수행한다.** "사람이 해야 한다" 를 추측으로 규정하지 않는다.
- ⚠️ **Host 의 UIPI 제약을 일반 Client 로 확대하지 않는다.** Host 는 `requireAdministrator` 라
  비관리자 세션에서 `PostMessage` 가 막히지만, **Client 는 사용자 권한 프로그램이라 같은 권한
  세션에서 실행·스크린샷·조작이 가능하다.** 2026-09-11 에 이 둘을 혼동해 UI 결함을 반나절 늦게
  발견했다.
  ⚠️ **"전부 가능" 이 아니라 "실증된 도구 범위 안에서" 다.** 지금까지 확인된 것은 UIAutomation ·
  `System.Drawing` 캡처 · WebView2 의 `ExecuteScript`/`CapturePreview` 이고,
  **WebView 내부에 대한 물리 마우스 클릭은 아직 실증되지 않았다.**
- **현재 있는 수단을 먼저 쓴다**: UIAutomation, WebView2 의 `ExecuteScript` + `CapturePreview`.
  Playwright·WebDriver·VM 은 **도구 설치가 필요하므로 기본값이 아니다.** 필요하면 근거와 함께 제안한다.
- **"누를 수 있다" 는 `DOM.click` 과 다르다.** 가시성·활성만이 아니라 **가림(`elementFromPoint`)·
  포커스 도달·화면 밖**을 함께 보고, **부정 대조가 실제로 실패하는지** 확인한다. 실패하지 않는
  부정 대조는 장식이다.
- 로그인 비밀번호가 없으면 **허용된 test account·fixture·isolated profile 로 갈 수 있는 데까지**
  간다. **비밀번호를 받아내려 하지 않는다.**
- 진짜 사용자만 할 수 있는 것(**UAC 승인 · 실기기 · 계정 로그인**)은 **준비를 전부 끝낸 뒤 정확히
  1회** 요청한다. UAC 가 필요하다고 **나머지 자동 작업을 멈추지 않는다.**
- **초기 테스트 환경을 세울 때의 승격**과 **제품이 추가로 요구하는 UAC 0회 측정 구간**을 구분해 적는다.
- ⚠️ **elevated test runner 나 VM 을 필수 선행조건으로 두지 않는다.** 필요하면 선택 후속으로 분리한다.
- 🔴 **자동화는 격리된 test build · fixture · 임시 프로필까지다.** 사용자가 **사용 중인 제품
  프로세스의 종료·재시작·설치·파일 교체는 명시 승인이 있을 때만** 한다. 승인 범위 안에서도
  **복구 기준선을 먼저 남긴다**: 프로세스 신원(pid·부모·생성 시각) · 설치 파일 해시 · 되돌릴 설치본.
  이 절이 "먼저 자동화해서 수행하라" 고 말하는 대상은 **격리된 것**이지 사용자가 지금 쓰고 있는
  기기가 아니다.

## 연속 수행 — 단계마다 멈추지 않는다

- 승인된 task의 필수 배선·오류 수정·검증·관련 기록·빌드·서명·허용된 자동 배포·외부 확인은
  **포괄 승인**이다. 단계마다 "진행할까요", "GO 대기"로 사용자에게 되묻지 않는다.
- **기능 단위로 구현·검사한다.** 매 commit마다 전량 검토·sweep·보고 관문을 만들지 않는다.
  작업용은 자체 검증 후 검사 묶음을 전달하고, 이미 승인된 독립 후속을 계속한다.
- 검증용은 요청문만 기다리지 않는다. commit·워킹트리·실행 상태를 확인해 **편집 중 파일과
  구분된 고정 후보**를 검사한다. 보고가 늦다는 이유로 병렬 중복 작업을 시작하지 않는다.
- pause는 **실제 승인 범위를 벗어난 변경·파괴성·새 권한·다른 계정/서비스 영향**의 해당 부분만 한다.
  이미 승인된 같은 조작에 재승인을 요구하지 않는다.
- 미완료 상태로 턴을 끝낼 때는 **실행 중인 담당자와 반환 이벤트**, 또는 **명시적 blocker와 해소
  담당자**가 있어야 한다. 정당한 이유 없이 "지시 주시면 시작" 상태로 놓아두지 않는다.
- task별 작은 상태 기록 하나에 **유효 revision·확인된 단계·마지막 실질 seq·미결 요청·다음 담당·
  사용자 최종 보고 여부**를 둔다. 새 버스·DB·복잡한 ACK 시스템부터 만들지 않는다.
- 자율 재개가 없는 환경에서는 돌아올 결과·인계를 실제로 연결한다. 정책만으로 무한 자율 실행을
  약속하지 않는다. **완료·답변 불필요 공지·사용자 필수 입력 대기는 메시지를 더 보내지 않아도 된다.**
- **무한 polling·의미 없는 재실행·ACK 왕복은 금지**한다. 상태 확인은 미결 조회 하나 규칙을 따른다.
- 세션이 바뀌면 역할 확인 후 **기존 task의 최신 revision을 한 번 인계**한다. 중복 task·키·게시 금지.
- **버스 completed는 실제 파일·로그·산출물과 별개다.** 검토와 사용자 최종 안내 없이 완료로 읽지 않는다.

## 범위와 질문

- 원 요청 행동을 **완성하는 데 필수인** 누락 배선·오류 처리·회귀·동작 보존 추출은 **기존 범위**로
  보고 검증용이 자율 지휘한다. (예전의 *"계획 밖 발견은 무조건 고치지 말 것"* 은 이 범위에서 개정됐다.)
- **protocol · 신뢰 경계 · 대형 구조 변경**은 Codex 확정이 필요하다.
- ⚠️ 이것은 **모호한 반려를 무시할 권한이 아니다.** 이견은 commit 과 워킹트리 근거로 조율하고,
  확정된 원계약과 사용자 우선순위를 유지한다.
- ledger(별건 기록) 항목을 연쇄로 늘리지 않는다.

## 검증 독립성과 재현

- 검사 대상을 **고정 commit + 워킹트리 상태 + candidate 파일 hash + 테스트 환경·입력 계약**으로
  적는다.
- **작업용 `build-local` 과 검증용 `build-verify` 의 출력을 공유하지 않는다.** 매번 전량 재빌드보다
  **고정 산출물을 독립 실행**하는 쪽을 먼저 쓴다.
- **무엇을 검사했는지 해시로 고정한다.** 같은 commit·같은 툴체인이 같은 바이너리를 보장하지 않으므로
  해시가 유일한 고정점이다. 다만 **두 경우를 구분한다**:
  - **출하 후보를 검사할 때**: 테스트한 HTML·바이너리가 **배포 candidate 와 같은 해시**여야 한다.
  - **product-equivalent test build 를 쓸 때**: **동일 source commit + 공유 UI 자산 해시**
    (예: `ui\shell.html`) **+ 명시된 build 차분 + artifact 별 해시 매핑**으로 범위를 고정한다.
    **테스트 exe 와 출하 exe 의 해시는 달라도 된다** — 위의 "테스트 빌드와 출하 빌드는 달라도 된다"
    가 허용하는 것이 정확히 그 차이다. 대신 **그 빌드가 증명하지 못하는 권한 항목을 함께 적는다.**
- ⚠️ **HEAD 만 보거나 워킹트리만 보고 "미착수" · "기존 코드 그대로" 라고 단정하지 않는다.**
- 테스트 계정·키·root·process handle·port/lock 을 격리한다. **저장소 밖 cleanup, 이름만 보고 하는
  kill, 전역 제품 락 경쟁은 하지 않는다.**
- 키·토큰 원문을 출력에 남기지 않는다.
- ⚠️ **UI 를 실행하지 않은 것을 "격리 실증" 이라고 부르지 않는다.**

## 결과를 읽는 법

- **PASS 개수는 그 프로세스가 정상 종료했을 때만 결과다.** `^PASS` 개수 · **종료 코드** ·
  마지막 요약 줄을 함께 읽는다. 하나라도 없으면 결과가 아니다 — 중단 지점과 완주가 똑같이 생긴다.
- **개수 비교는 같은 구성끼리만** 한다. 항목이 바뀐 두 실행의 총계를 맞대지 않는다.
- **간헐 결함은 1회 통과로 닫지 않는다.** 반복 실행으로만 보인다.
- **"정적 검사로 확인됨" 을 "동작 확인됨" 으로 쓰지 않는다.**

## 역할별 책임 — 중계보다 실행·측정

- **제품 작업**: Codex는 목표·완료 기준·중요 설계·최종 보고를 맡는다. 작업용은 한 기능을 구현하고
  **자체 사용자 행동 검증까지** 한다. 검증용은 고정된 후보를 독립 실행·측정해 실제 결과를 확인한다.
- 검증용은 매번 사양을 다시 쓰거나 사소한 결정을 Codex에 올리지 않는다. **구체적인 결함의 반려와
  같은 범위 재수정 요청 권한은 유지**한다. 모든 판단을 Codex에 몰아 새 병목을 만들지 않는다.
- **문서·정책 전용**: 검증용 직접 편집 → Codex 전문 검토 → 검증용 기록·Git MCP 커밋 →
  Codex 최종 보고. **검증용 자기 최종 승인과 작업용 경유를 금지**한다.
  제품 변경에 딸린 문서는 작업용이 함께 기록한다. 사용자 지시로 Codex가 직접 수행하는 범위는
  예외이며 별도 Claude 왕복을 만들지 않는다.
- 제품 검증용은 제품 코드를 직접 수정하지 않는다. 검토 기록은 미추적 위치에 둔다.
  역할을 나눈 것 자체나 같은 테스트의 반복을 독립성의 증거로 삼지 않는다.
- 검사는 **고정 후보·환경·생산 의존성 공급·실제 실행 결과**를 기준으로 한다. stub과 제품 구성의
  차이를 먼저 확인한다. 검증 후 변경되면 **영향받는 범위만** 다시 확인하고 근거를 갱신한다.
- 주소는 displayName과 a2a_whoami로 확인하고 **identity+sessionId**로 pin한다. 일시적 ID를
  정책에 고정하지 않는다. 최종 형식은 위의 최우선 보고 규칙만 따르고 별도 장문 보고를 추가하지 않는다.

## 로그를 읽는 법

- **사용자가 증상을 보고하면 추측하지 말고 먼저 로그를 연다.** 로그로 확인하기 전에 원인·수정
  방향을 단정하지 않는다.
- **무엇이든 보기 전에 출처를 먼저 고정한다**: **기기 · 시각 · 버전 · 서버 주소 · 스트림**.
  이것을 고정하지 않은 로그 비교는 근거가 아니다.
- **NAS(디렉터리 서버)가 우선이다** — viewer/host/client 가 모두 거기로 올라가므로 **여러 기기의
  같은 사건을 같은 출처로 대조**할 수 있다. 원격 뷰어의 `viewer.log` 는 이 PC 에 아예 없다.
  - 접속: `ssh claude@192.168.0.6` (읽기 전용 계정). 경로는 디렉터리 서버
    (`apps/directory/server.js`)의 `<LOG_DIR>/<account>/<device>/<stream>.log`.
  - ⚠️ **주소·계정은 설정/메모리에서 읽는다.** 여기 적힌 값을 현재 진실로 하드코딩하지 않는다.
- **로컬 로그도 본다**(예전의 *"로컬 사본을 보지 말 것, 예외 금지"* 는 **삭제됐다**). 다음은 NAS 에
  없거나 늦게 도착한다:
  - 실패한 기기의 원본 로그, **업로더가 붙기 전에 난 실패분**
  - `C:\Program Files\GNLink.update\updater.log` — 업데이트 판정의 정본
  - Windows 이벤트 로그
- ⚠️ **NAS 에 모였다는 것이 시계가 맞는다는 뜻은 아니다.** 시각 대조에는 각 기기의 시계 오차를 감안한다.
- ⚠️ **로그가 없다고 "실행되지 않았다" 로 확정하지 않는다.** 미보존·회전·버퍼 없음·경로 차이·쓰기
  실패가 모두 같은 모양으로 보인다. **가능성**으로 적는다.
- ⚠️ **다른 기기의 설정 파일로 이 기기의 원인을 단정하지 않는다.** (2026-09-11 실제 오류: 이 PC 의
  `client.txt` 값으로 회사 PC 원인을 추론한 것)

## 릴리스 배포 (NAS)

- **배포는 `gnlink` 계정으로 이 PC 에서 직접 한다.** 키는 `~/.ssh/remote60_deploy`,
  접속은 `ssh -i ~/.ssh/remote60_deploy gnlink@192.168.0.6`. 키 전용·LAN 한정, 홈은 `/opt/gnlink`.
- 이 계정이 **소유**하므로 sudo 없이 되는 것: `/opt/gnlink/updates/<버전>/` 생성과 아티팩트 게시,
  `/opt/gnlink/update-manifests/{windows,android}.{manifest,sig}` 교체.
- **sudo 가 없어 못 하는 것**: nginx 설정, systemd 유닛·dropin(=env 변경), ACL, 패키지 설치.
  최초 구축(0.2.108) 때 끝났으므로 평시 릴리스에는 필요 없다.
- **손이 아니라 스크립트로 한다** — `automation/gnlink_deploy.sh` 가 절차의 구현이다.
  ```
  automation/gnlink_deploy.sh --release-dir .claude/rel/<버전> --verify-only   # 남의 서버 상태를 처음 볼 때
  automation/gnlink_deploy.sh --release-dir .claude/rel/<버전> --dry-run
  automation/gnlink_deploy.sh --release-dir .claude/rel/<버전>
  ```
  순서는 고정이다: **preflight → 아티팩트 업로드 → 서버에서 검증 → 백업 → manifest+sig 교체(마지막)
  → 외부 https 확인.**
- ⚠️ **manifest 와 sig 의 교체는 "pair 원자적" 이 아니다.** 둘을 `.tmp` 로 올린 뒤 **`mv` 를 두 번**
  한다. `rename(2)` 은 **파일 하나 단위로만** 원자적이고, 스크립트 자신이 그렇게 적고 있다
  (`automation/gnlink_deploy.sh:435-436`). 즉 **두 `mv` 사이에 manifest 와 sig 가 짝이 맞지 않는
  창이 있다.** 그 창은 업로드 전체 길이가 아니라 rename 두 번 폭으로 **좁혀진 것**이지 없어진 것이
  아니다. 그 사이에 읽은 클라이언트는 서명 불일치로 **거부**한다(안전한 방향이다). 이 사실을
  "원자적" 으로 바꿔 적지 않는다.
  - 🔴 **부분 교체 실패는 자동 원복이 보장되지 않는다.** 두 rename 은 `mv && mv` 로 이어져 있어
    (`:440`), **첫 번째만 성공하면 manifest 는 새 것 · sig 는 이전 것**으로 남는다. 뒤이은
    `rm -f *.tmp` 는 그 상태를 되돌리지 못한다. 이때는 **정확한 backup pair 로 직접 원복**해야 하고,
    게시 실패는 **blocker 로 보고**한다.
    ⚠️ 스크립트의 `die` 문구 *"the previous release is still published"*(`:442`)는 **이 경우에 맞지
    않는다** — 운영 한계로 기록해 둔다.
  - **후속 2건**(제품 스크립트라 이번 문서 작업에서 건드리지 않음): 위 `:442` 실패 메시지,
    그리고 `:445` 의 로그 문구가 아직 *"env 를 바꿀 때만 재시작"* 취지라 아래 재시작 항목과 어긋난다.
- **재시작**: 평시 릴리스는 필요 없다 — manifest 핸들러가 요청마다 디스크를 읽는다
  (`apps/directory/server.js`). ⚠️ 다만 *"env 를 바꿀 때뿐"* 은 틀렸다. **서버 코드나 그 의존 모듈을
  갱신할 때도 재시작이 필요하다.** 서버앱을 올릴 때는 **의존 모듈 전량**(`server.js` ·
  `update_manifest.js` · `version_compare.js` · `package.json`)을 함께 올린다 — 예전에
  *"server.js 단독"* 으로 올려 첫 재시작이 `MODULE_NOT_FOUND` 였다.
- 아티팩트는 **버전별 불변 경로**에 둔다. 기존 버전 디렉터리를 덮어쓰거나 지우지 않는다 —
  manifest 가 가리키는 URL 의 바이트가 나중에 달라지면 서명이 무의미해진다.
- **롤백은 `.manifest`+`.sig` 두 파일을 정확한 짝으로 원복**하는 것이 전부다. 아티팩트는 그대로 둔다.
  ⚠️ 백업 디렉터리 이름만 보고 고르지 않는다 — artifact 수가 모자라 **swap 을 끝낼 수 없는 문서**가
  섞여 있다.
- 게시 전후로 **문서와 디스크의 해시가 같은지** 따로 확인한다. 서명 검증은 *"문서가 서명 이후 바뀌지
  않았다"* 만 말하고 *"문서가 실제로 존재하는 파일을 가리킨다"* 는 말하지 않는다.
- ⚠️ **무인증 401 만으로 업데이트 성공을 검증하지 않는다** — 잠금장치를 확인한 것이지 방 안을 본 것이 아니다.
- `gnlink` 로 불가능한 작업은 **`ESCALATE` 로 구체 조작을 출력하고 exit 3**. 그 출력을 그대로 NAS
  세션에 넘긴다. **`sudo` 를 우회하지 않는다.**
- 회귀는 `automation/gnlink_deploy_test.sh`. ⚠️ **ssh 와 curl 은 이 회귀가 대신하지 못한다.**
- ⚠️ **사실을 확인하지 않은 채 정적 정책으로 고정하지 않는다.** 이 절의 문장이 스크립트 동작과
  어긋나면 **스크립트를 근거로 문서를 고친다.**

## 기타

- 워크플로우·커밋 규칙은 `AGENTS.md` 를 따른다.
