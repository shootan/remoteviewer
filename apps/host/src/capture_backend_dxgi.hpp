#pragma once

namespace remote60::host {

// Low-priority backlog scaffold:
// Future Desktop Duplication backend (Parsec-style path) will be implemented here.
// Intentionally not wired to runtime yet.
struct DxgiCaptureBackendPlan {
  bool planned = true;
};

}  // namespace remote60::host
