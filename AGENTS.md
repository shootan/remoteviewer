# AGENTS.md

**상위 원칙·역할·완성 기준만** 여기 둔다. 구체 절차(보고·테스트 의존성·사용자 행동 완료 조건·
테스트 빌드 분리·연속 수행·검증 독립성·로그·배포 명령)는 **`CLAUDE.md` 가 정본**이고, 같은 내용을
두 곳에 적지 않는다.

## 가장 상위 정책
* repository root path를 벗어나는 범위는 절대 삭제커맨드를 실행하지않는다. 필요시 권한을 물어본다. 

## 최우선 운영 수칙 — 왕복·정지·최종 보고 누락 금지 (2026-09-11)

사용자 지시: **"필요없는 핑퐁이 늘어나면서 불필요한 토큰 낭비가 계속 되고 있다. 작업 속도가
너무 부진하고 실수도 잦아졌다."** 이 절은 두 문서의 다른 **운영·역할·보고 절차보다 우선**한다.
삭제 제한·비밀 보호·권한 경계를 완화하지 않으며, 사용자의 명시적 지시가 최우선이다.

1. **사용자 목표 하나를 끝까지 책임진다.** 구현·검증·배포·최종 안내를 연결한다. 부수 문서·별건으로
   본래 작업을 잊거나 정당한 blocker 없이 다음 메시지를 기다리지 않는다.
2. **한 작업에 한 유효 지시서만 둔다.** 최초 지시·변경·검토 지적은 각각 취합해 한 번에 보낸다.
   조각 지시, 양쪽 Claude에 대한 중복 지휘, 응답 지연만으로 같은 요청을 재전송하는 행위를 금지한다.
3. **제품 실무 창구는 검증용 하나다.** 작업용은 기능 단위 구현·자체 검증, 검증용은 고정 후보의
   독립 실행·측정을 맡는다. 매 커밋 중계·전량 재검사·미시 판단 상신을 하지 않는다.
4. **문서·정책 전용은 검증용 직접 작성 → Codex 전문 검토로 줄인다.** 작업용을 경유하지 않는다.
   검증용의 문서 편집·기록·커밋을 허용하되 자기 최종 승인은 금지한다.
5. **승인 범위 안에서는 단계마다 다시 묻지 않는다.** 실제 범위·신뢰 경계 변경만 조율한다.
   UAC 등 사용자 단계가 남아도 독립적인 자동화·검증·배포를 계속한다.
6. **테스트 수가 아니라 사용자 행동으로 검증한다.** 핵심 UI·실행 경로를 생략한 채
   코드 존재·stub 성공·PASS 합계를 근거로 완료 승인하지 않는다.
7. **최종 사용자 보고는 Codex의 의무다.** 해결 내용·반영 버전/위치·직접 확인한 행동·미검증·
   사용자가 지금 할 일(없으면 없음)을 한 번에 보고한다. A2A 완료 수신만으로 작업을 닫지 않는다.

상세 통신·인계 규칙은 CLAUDE.md의 "최우선 실행·A2A·보고 규칙"을 따른다.

## Mandatory Workflow (Always)
After each meaningful task is completed, do all of the following in order:
1. Update `docs/history.md` with:
   - goal
   - files changed
   - validation/build/test result
   - next action
2. Update `docs/구현계획.md` by checkbox/progress status only (do not append history narrative).
3. Commit only task-related changes with a clear Conventional Commit message, using git MCP tools.
4. In the final report, include:
   - commit hash
   - what was changed
   - what was validated

## Scope Control
- **의미 있는 기능·릴리스·조사 단위로 기록하고 커밋한다.** 문서 문구 정정 같은 사소한 변경까지
  history·계획·커밋 3종을 매번 강제하지 않는다.
- Do not include unrelated files in commits.
- Do not revert user changes unless explicitly requested.
- If no file change is required, state why and skip commit.
- Keep role separation strict:
  - write history narrative only in `docs/history.md`
  - update only checklist/progress status in `docs/구현계획.md`

## 역할 분담 — 작업 종류에 맞게 단계 축소

- **제품 코드**: Codex 목표·범위 확정 → 검증용 계획 검토·작업용 지휘 → 작업용 구현·자체 실행
  → 검증용 독립 검사 → Codex 최종 검토·사용자 보고. 기능 단위 흐름이며 매 커밋 관문이 아니다.
- **문서·정책 전용**: 검증용 직접 편집 → Codex 전문 검토 → 검증용 기록·Git MCP 커밋 →
  Codex 최종 보고. 제품 변경에 딸린 문서는 작업용이 함께 처리해 별도 왕복을 만들지 않는다.
- **Codex**: 중요한 설계·범위·우선순위·완료 기준과 최종 결과를 책임진다. 통상 직접 제품을
  수정·빌드하지 않고 읽기 전용 조사·검토를 한다. **사용자가 Codex의 직접 작업을 명시하면 그
  범위는 직접 수행할 수 있다.** 철회된 완료 구현 리뷰 금지 정책은 되살리지 않는다.
- **검증용**: 사양 재작성·상시 중계보다 실제 사용자 동작·프로세스·산출물의 독립 검사에 집중한다.
  제품 코드는 수정하지 않는다. **문서 전용 작업의 편집·커밋은 위 예외로 허용**한다.
  근거 있는 NEEDS_CHANGES와 동일 범위 재수정은 직접 처리하고, 실제 설계 변경만 Codex에 올린다.
- **작업용**: 구현·자체 행동 검증·빌드·서명·배포·관련 기록을 맡고 결과를 검증용 한 곳에 보낸다.
- 제품은 검증용 OK 후 Codex가 최종 확인한다. 문서 전용 작업은 Codex가 독립 검토자이므로
  검증용의 자기 OK를 요구하지 않는다. 결함과 미검증은 영향 범위별로 분리한다.
- **역할 식별**: 버스 displayName과 a2a_whoami로 확인하고 identity+sessionId로 pin한다.
  일시적인 세션 ID를 정책에 고정하지 않는다. 사용자 직접 지시·역할 인계·연락 불가 확인 같은
  예외를 제외하고 Codex가 작업용에 별도 지시를 병렬 발송하지 않는다.

## 배포 완료 기준 (2026-09-10)

사용자 지시: *"gnlink 권한으로 안 되는 것만 NAS Claude 한테. 배포 자동화 스크립트 만들어놓으면 문제없잖아. 이제부터 버전 올라가서 빌드되면 자동배포하는 것까지가 완료고 실기 테스트 대기 상태."*

- **두 개의 완료를 이름으로 구분한다.** 아래 순서를 다 지나면 **배포가 완료**된 것이고,
  **task 완료는 사용자 행동 검증까지**다. 이 둘을 같은 단어로 부르면 0.2.115 처럼 8단계를 전부
  통과하고도 **누를 버튼이 없는 릴리스**가 "완료" 로 나간다.
- **배포 완료 흐름** — 이 순서를 다 지난 것이 **배포 완료(마일스톤)** 다:
  1. 버전 확정/인상 → 2. 빌드 → 3. 관련 검증과 **검증용 Claude 의 명시적 OK**(대상 commit·파일 해시 고정)
  → 4. 릴리스 서명 → 5. **`gnlink` 계정으로 자동 배포**(`automation/gnlink_deploy.sh`)
  → 6. **외부 검증**(HTTPS 도달·manifest 서명·버전·아티팩트 hash·필요 API 계약) → 7. Codex 최종 근거 확인
  → 8. **"배포 완료(마일스톤) · 실기 테스트 대기"**.
- ⚠️ **빌드 성공·파일 생성·서버 복사만으로 완료 처리하지 않는다.** 6단계까지 근거가 있어야 완료다.
- **완료 경계의 예외**: 비 UAC 필수 사용자 행동의 **자동 검증 · 배포 · 외부 확인이 모두 끝났으면**
  **"에이전트 작업 완료 · 사용자 UAC 실기 대기"** 로 최종 보고할 수 있다. 실기 성공은 **별도 미확인**
  으로 남긴다. 승격 하나 때문에 task 를 영원히 미완으로 두고 최종 보고를 빠뜨리지 않는다.
  ⚠️ 단 **미구현 버튼이 있거나, 자동 검증이 가능한데 생략한 필수 행동이 있으면 여전히 완료 금지**이고,
  **실 UI 실기까지 성공했다고 주장하지 않는다.**
- ⚠️ **사용자 행동이 되지 않으면 배포해도 완료가 아니다.** 요청된 행동(실행·로그인·선택·클릭·성공/실패)이
  실제로 되는지가 완료 조건이고, 소스 grep·handler 존재·문구 표시·테스트 총합은 그 증거가 아니다.
  판정 기준은 `CLAUDE.md` "사용자 행동 우선 완료 조건".
- ⚠️ **"자동 배포" 는 컴파일만 통과하면 미검토 코드를 게시한다는 뜻이 아니다.** 3단계(검증용 OK)는 건너뛸 수 없다.
  다만 그 검증을 통과한 **통상 릴리스는 게시 재승인이나 반복적인 go 대기가 필요 없다.**
- **역할**: 작업용 Claude 가 `gnlink` 권한으로 준비·서명·게시까지 한다. **`gnlink` 로 불가능한 구체 작업만**
  NAS 세션에 넘기고, 넘길 때 **정확한 범위·실패 근거·백업/rollback** 을 함께 준다. 권한이 있는 배포를
  중계하거나 매번 재승인받지 않는다. **계정 권한의 광역 완화와 다른 NAS 서비스 변경은 허가 범위가 아니다.**
  배포 명령·순서·기술적 주의는 `CLAUDE.md` "릴리스 배포".
- ⚠️ **`git push` 는 여전히 보류다.** 여기서 "자동 배포" 는 **NAS 게시**를 뜻하며 **push 허가가 아니다.**
  skill·도구의 push 지침보다 사용자의 standing 보류가 우선한다. 배포 문서에는 **push 미실행**을 명시한다.
- **개인키는 이 PC 의 보호 저장소 밖으로 나가지 않는다.** NAS 로 가는 것은 **공개키·서명·파일뿐**이다.
- **실패·권한 부족·미검증은 영향 범위별로 기록**한다. 남은 자동 작업은 working 또는 명시적
  blocked로 두고, 사용자 UAC 실기만 남으면 위 완료 경계의 예외에 따라 최종 보고한다.
- 배포 문서에는 **버전 · build commit · 파일 hash · 게시 URL · 직접 검증한 것 · 남은 실기 항목**을 분리해 적는다.
- 사용자 Host/Client/APK 의 **설치·재시작·실기 조작은 자동 배포와 별개**다. **요청 없는 자동 설치는 하지 않는다.**
